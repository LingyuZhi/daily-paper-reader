---
title: Knowledge- and Gradient-Guided Reinforcement Learning for Parametrized Action Markov Decision Processes
title_zh: 参数化动作马尔可夫决策过程中的知识与梯度引导强化学习
authors: "Jonas Ehrhardt, René Heesch, Oliver Niggemann"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12924v2"
tags: ["query:agentic-rl"]
score: 8.0
evidence: 对参数化动作马尔可夫决策过程进行强化学习，直接处理智能体决策
tldr: 参数化动作马尔可夫决策过程（PAMDP）中，现有强化学习算法因使用单次参数估计而样本效率低。本文提出知识梯度引导强化学习（KGRL），利用Datalog知识库推导适用动作和可行参数，剪枝非适用动作并约束参数空间，结合梯度参数精化循环估计最优参数。实验表明，KGRL在多个PAMDP环境显著提升样本效率和回合回报，超越基线方法。KGRL首次将显式但不完整的领域知识有效集成，并提供了局部程序性解释。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12924v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12924v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 690, \"height\": 687, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12924v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1777, \"height\": 446, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12924v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 768, \"height\": 577, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12924v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 1982, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12924v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 727, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12924v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1811, \"height\": 708, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12924v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 830, \"height\": 340, \"label\": \"Table\"}]"
motivation: 现有PAMDP强化学习算法样本效率低，且未充分利用可用的领域知识来指导探索和决策。
method: KGRL使用Datalog知识库推导适用动作和可行参数，剪枝非适用动作并约束参数空间，再通过梯度参数精化循环优化参数。
result: 在PAMDP基准任务上，KGRL在样本效率和回合回报上均超越现有强化学习基线。
conclusion: KGRL有效将领域知识融入强化学习，提高样本效率，并提供可解释的决策过程。
---

## 摘要
本文研究参数化动作马尔可夫决策过程（PAMDP）中的强化学习，其中每个决策由一个符号动作和数值参数组成。在此类设置中，强化学习算法通常使用一次性估计器确定参数，导致训练样本效率低下。尽管大多数PAMDP环境中存在显式但不完整的知识（例如规则、安全约束或专家启发式），但极少直接用于提高训练强化学习智能体的样本效率。我们填补了这一空白，提出了一种新颖的神经符号知识与梯度引导强化学习（KGRL）算法。KGRL利用Datalog知识库中的领域知识，推导出给定状态下的适用动作集合和可行参数。这使得它能够从决策空间中剪除不适用动作，并约束剩余动作的参数空间。然后，我们使用基于梯度的参数精化循环来估计智能体训练和部署期间的最优参数。通过沿轨迹记录激活的规则，KGRL还提供了关于动作剪枝和参数约束的局部过程性解释。总体而言，KGRL引导智能体朝向可行且感知约束的决策进行探索和部署，同时在训练期间提高样本效率。在PAMDP的样本效率和回合回报方面，KGRL均优于最先进的强化学习基线。

## Abstract
In this paper, we study Reinforcement Learning in Parametrized Action Markov Decision Processes (PAMDP), where each decision consists of a symbolic action and numerical parameters. In such settings Reinforcement Learning algorithms typically determine parameters with one-shot estimators, which makes their training sample inefficient. Though in most PAMDP environments explicit but incomplete knowledge (e.g., rules, safety constraints, or expert heuristics) is available, it is rarely directly used to increase the sample-efficiency of training Reinforcement Learning agents. We step into this gap and propose our novel Neuro-Symbolic Knowledge- and Gradient-Guided Reinforcement Learning (KGRL) algorithm. KGRL uses domain knowledge in a Datalog knowledge base to derive the set of applicable actions and feasible parameters for a given state. This allows it to prune non-applicable actions from the decision-space and constrain the parameter spaces of the remaining actions. We then use a gradient-based parameter refinement loop to estimate the optimal parameters during training and deployment of the agent. By recording activated rules along the trajectory, KGRL additionally provides local procedural explanations on the pruning of actions and constraining of parameters. Overall, KGRL guides the agent's exploration and deployment toward feasible and constraint-aware decisions, while increasing sample efficiency during training. KGRL outperforms state-of-the-art RL baselines for PAMDPs in both, sample efficiency and episodic return.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在参数化动作马尔可夫决策过程（PAMDP）中，每个决策包含一个符号动作和一个连续数值参数。现有强化学习算法通常使用一次性参数估计器（one-shot estimator）来确定参数，导致训练样本效率低下。虽然许多PAMDP环境具有显式但不完整的领域知识（如规则、安全约束或专家启发式），但很少被直接用来提高样本效率。
- **核心问题**：如何将显式但不完整的领域知识融入强化学习的训练和部署，让符号推理执行已知约束，同时让学习优化无法符号指定的部分？
- **整体含义**：本文旨在将符号知识库与梯度引导的参数优化相结合，在参数化动作空间中实现更高效、更安全的强化学习。

## 2. 论文提出的方法论

- **核心思想**：提出神经符号知识与梯度引导强化学习（KGRL）算法，基于DQN扩展，引入两个核心组件：
  - **Datalog知识库**：在每个决策步骤，将当前数值状态通过抽象函数α映射为符号状态表示，结合静态动作声明和约束声明，推导出适用动作集 Ā(s) 和每个适用动作的可行参数空间 {Ψ̄a(s)}，从而剪除非适用动作并约束参数范围。
  - **梯度引导参数精化（PARAM OPT）**：基于动作值函数Qθ对参数ψ的梯度，进行投影梯度上升（projected gradient ascent），从初始参数猜测开始迭代优化，直到梯度范数小于阈值ξ或达到最大步数Umax，输出精化后的参数估计ψ̃*。
- **算法流程（文字说明）**：
  1. 初始化在线DQN、目标DQN、经验回放缓冲D和环境。
  2. 对于每一步t，评估知识库K和符号状态α(s)，得到 Ā(s) 和 {Ψ̄a(s)}。
  3. 如果探索（随机），从 Ā(s) 中随机选动作并从 Ψ̄a(s) 中随机采样参数；如果利用，对每个适用动作调用PARAM OPT得到精化参数估计，然后贪心选择Q值最高的动作-参数对。
  4. 执行动作，获取奖励、下一状态、终止标志，存入回放缓冲。
  5. 定期从缓冲采样小批量，使用目标DQN计算目标值y（注意计算目标时同样需要评估知识库并调用PARAM OPT），通过均方贝尔曼误差更新在线DQN参数。
  6. 周期性地同步目标DQN权重。
- **关键公式**：
  - 知识约束的PAMDP：`Ā_K(s) = {(a, ψ) | a ∈ Ā(s), ψ ∈ Ψ̄a(s)}`
  - 目标值计算：`y = r + γ(1-d') * argmax_{a'∈Ā(s')} max_{ψ'∈Ψ̄_{a'}(s')} Q_{θ^-}(s', a', ψ')`
  - PARAM OPT的更新：`ψ̂ ← Proj_{Ψ̄a(s)}(ψ̂ + ζ * ∇_{ψ̂} Q_θ(s, a, ψ̂))`

## 3. 实验设计

- **场景/数据集**：使用四个经典PAMDP基准域：
  - **HardMoveX**（2D导航）：X=4,8,10，动作空间大小可调。智能体需选择moveX(ψ)动作指向目标方向，参数为移动幅度[0,1]。
  - **CatchPoint**（2D抓取任务）：智能体可移动(ψ∈[-1,1])和抓取(ψ∈[0,1])，参数映射为角度。
  - **HardGoal**（简化的足球任务）：10个射门动作a0~a9，参数ψ∈[-1,1]，守门员连续移动。
  - **Platform**（平台穿越任务）：可跑步(ψ∈[0,30])、跳跃(ψ∈[0,720])、弹跳(ψ∈[0,430])。
- **Benchmark**：每个域都手工设计了简单的规则集，用于KGRL的知识库。
- **对比方法**：五种PAMDP强化学习基线——QPAMDP、PA-DDPG、P-DQN、HyAR、MP-DQN。此外，还对KGRL本身进行消融实验（去除知识库、去除梯度引导、或两者都去除，且区分仅在训练时、仅在评估时、或两者都去除）。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量或训练时长细节。仅提到固定预算为500,000训练步和最大计算墙时间12小时，以及8个不同种子重复训练。根据致谢，使用了HSUper HPC集群（hpc.bw项目），但未给出具体硬件配置。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：在6个子域（HardMove4/8/10、CatchPoint、HardGoal、Platform）上对比KGRL与5个基线，每个域在8个种子上重复，每1000步执行16个不同初始条件的评估问题，报告AULC（曲线下面积）。
  - 消融实验：对KGRL进行10种消融变体（不同组合的训练/评估时去除知识库和梯度引导），在同样6个子域上报告AULC。
  - 知识库利用率分析：报告了K-utilization和A-pruning两个指标（均值±标准差，8个种子）。
  - 解释轨迹示例：HardGoal域的一个示例热力图。
- **实验充分性**：实验比较全面，覆盖了不同动作空间大小、不同任务类型，消融设计细致区分了训练与评估阶段的影响。遵循了Vranješ等人提出的实证机器学习研究设计指南。结果呈现了均值、标准差和学习曲线，具有较好的客观性和公平性。但未进行统计显著性检验（如t检验），仅在表1中给出均值±标准差。

## 6. 论文的主要结论与发现

- KGRL在所有评估域上均取得了最高的平均AULC，且起始回报更高、收敛更快、方差更低（相比所有基线）。
- 知识库主要在**评估**阶段发挥关键作用：去除知识库仅在评估时导致性能大幅下降；去除知识库仅在训练时影响较小。
- 梯度引导参数精化（PARAM OPT）在**评估**时有益，但在**训练**时反而有负效应（引入额外噪声，尤其当DQN尚未充分拟合时）。
- 同时去除知识库和梯度引导（训练和评估）性能最差，表明两者共同作用的重要性。
- 知识库利用率非平凡：在HardMove域几乎100%利用且剪枝约一半动作；CatchPoint和HardGoal利用率约1/3；Platform主要产生参数约束而非动作剪枝。
- KGRL可以记录规则激活作为局部过程性解释轨迹，支持可解释性。

## 7. 优点

- **创新性**：首次将结构化、可查询的知识库直接集成到PAMDP强化学习的训练和参数估计中，实现神经符号融合。
- **样本效率高**：通过剪枝非适用动作和约束参数空间，显著减少了无效探索，提高了样本效率。
- **性能优越**：在多个标准基准上超越5种现有方法，且方差低、收敛快。
- **可解释性**：通过记录规则激活提供了局部过程性解释，有助于安全关键应用。
- **消融设计细致**：区分训练/评估阶段去除组件的影响，揭示了不同组件的不同作用时机，为后续研究提供了重要洞察。
- **开源实现**：提供了可复现代码（GitHub链接）。

## 8. 不足与局限

- **对错误知识的敏感性**：硬剪枝（hard pruning）的方式使得错误或不当的规则会直接损害性能，无法自适应修复。作者提到可以使用自适应机制或软策略增强来缓解。
- **梯度引导在训练时的负效应**：PARAM OPT在训练时引入噪声，导致性能下降，尚未给出完全令人满意的解决方案（仅建议课程学习）。
- **全局最优不保证**：梯度引导属于局部优化，非凸的Q值函数下无法保证收敛到全局最优参数。
- **可解释性局限于局部过程性**：当前仅能提供单个决策的解释，缺乏全局模式聚合。
- **实验覆盖有限**：
  - 仅使用4个模拟域，均为低维连续动作空间（参数维度1~3），未涉及真实物理环境或高维参数空间。
  - 未与其他神经符号强化学习方法（如Veronese 2026、Han 2026）直接对比，他们仅适用于离散域。
  - 未进行大规模统计假设检验（如Wilcoxon符号秩检验）。
- **资源和算力细节不足**：未明确训练所需GPU型号或时间，虽然提供了最大墙时限制，但不利于复现效率评估。
- **抽象函数和知识库的手工依赖**：当前使用手工设计的规则和阈值抽象，若扩展到复杂域，学习知识库和抽象函数本身是一个开放挑战。作者虽提到可学习，但未实现和验证。

（完）
