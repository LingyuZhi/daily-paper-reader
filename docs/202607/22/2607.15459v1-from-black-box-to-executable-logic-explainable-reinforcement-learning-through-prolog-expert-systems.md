---
title: "From Black Box to Executable Logic: Explainable Reinforcement Learning through Prolog Expert Systems"
title_zh: 从黑盒到可执行逻辑：通过Prolog专家系统实现可解释强化学习
authors: Eduardo C. Garrido-Merchán
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15459v1"
tags: ["query:agentic-rl"]
score: 8.0
evidence: 将深度强化学习策略事后转化为可解释的Prolog程序
tldr: 深度强化学习策略是黑盒，本文提出将神经网络策略转化为可读、可运行的Prolog逻辑程序。通过三阶段方法：冻结PPO教师模型、归纳有序规则列表、输出Prolog程序，并后续进行规则编辑优化。离散任务（两室钥匙门）达到精确最优回报，连续任务Acrobot和CartPole接近教师性能，而LunarLander受限于指数下界。该工作提供了可解释、可验证、可编辑的规则化表示，并给出了理论保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15459v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1575, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 344, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1520, \"height\": 555}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1519, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 977, \"height\": 487}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1722, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1449, \"height\": 484}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15459v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1580, \"height\": 209}]"
motivation: 将深度强化学习的黑盒策略转化为可读、可运行的逻辑程序，实现可解释性并便于人工编辑。
method: 三阶段：冻结PPO教师，归纳有序规则列表，输出Prolog程序；后续通过策略评估迭代编辑规则以提升回报。
result: "离散任务精确最优，连续任务Acrobot/CartPole接近教师（约97%），LunarLander部分恢复，符合指数下界预测。"
conclusion: 提供了转换的理论保证（回报损失界、单调改进），实验验证了在有限状态和连续任务上的有效性。
---

## 摘要
训练好的深度强化学习策略是一个黑盒，我们探究能否将其重写为可执行的逻辑程序，使其行为可被人类阅读、逻辑引擎运行、优化器编辑，从而实现可解释性。我们提出一种三阶段事后转换方法：提取冻结的近端策略优化教师模型，以经典关系学习方式从其决策中归纳有序规则列表，并将结果输出为Prolog程序，该程序的每一个决策均由现成的逻辑引擎执行；后续扩展阶段编辑规则库，且仅在策略评估认证回报提升时才接受编辑。我们证明了四个保证。回报损失界使得蒸馏出的程序在有限马尔可夫决策过程中成为机器可验证的证书，扩展循环单调改进并终止。针对连续观测设置，我们回答了转换是否可能：命题阈值实例化将网络转换为任意保真度，随着分辨率B增长，不一致性为O(1/B)，回报差距以相同速率闭合，匹配下界表明对于斜决策边界，代价随观测维度指数增长。在具有16944个可达状态的两室钥匙门任务中，扩展后的Prolog程序在每个随机种子下均达到精确最优回报，且在预算受限机制下，10个种子中的全部10个在精确回报上超越随机教师。在三个连续控制任务中，输出程序替代网络：在Acrobot上以11条子句匹配神经教师（噪声范围内），在CartPole上恢复约97%的回报，而在需要更精细控制的LunarLander上仅部分恢复，这正是指数下界所预测的上限。

## Abstract
A trained deep reinforcement learning policy is a black box, and we ask whether it can be made explainable by rewriting it as an executable logic program that reproduces its behaviour and that a person can read, a logic engine can run, and an optimizer can edit. We present a three-stage post-hoc transformation that extracts a frozen proximal policy optimization teacher, induces an ordered rule list from its decisions in the manner of classical relational learning, and emits the result as a Prolog program whose every decision is executed by an off-the-shelf logic engine; a subsequent expansion stage edits the rule base and accepts an edit only when policy evaluation certifies a return increase. We prove four guarantees. A return-loss bound makes the distilled program a machine-checkable certificate in a finite Markov decision process, and the expansion loop improves monotonically and terminates. For the continuous-observation setting we answer whether the conversion is possible at all: the propositional threshold instantiation converts the network to arbitrary fidelity as the resolution B grows, with disagreement O(1/B) and a return gap that closes at the same rate, and a matching lower bound shows the cost is exponential in the observation dimension for an oblique decision boundary. Empirically, on a two-room key-and-door task with 16,944 reachable states the expanded Prolog program attains exact optimal return in every seed and, in a budget-capped regime, exceeds the stochastic teacher on exact return in ten of ten seeds. On three continuous-control tasks the emitted program substitutes the network, matching the neural teacher within noise on Acrobot with eleven clauses and recovering about 97% of its return on CartPole, while on the finer-control LunarLander it recovers only partially, exactly the ceiling the exponential lower bound predicts.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机与背景**：深度强化学习训练得到的策略是一个稠密的数值黑盒（例如神经网络），难以直接解释。传统的可解释方法（如显著性图、代理分数、反事实探测）无法生成人类可读、可执行、可编辑的制品。本文提出将冻结的PPO策略事后转化为一个可读、可运行的Prolog逻辑程序，该程序不仅保留了教师的行为，还能通过精确策略评估进行编辑优化，从而在性能上匹配甚至超越原始网络。
- **整体含义**：该工作证明了符号化事后解释可以与高性能并存——通过将神经网络策略蒸馏为有序决策列表（一阶逻辑程序），并利用精确回报进行扩展性编辑，可以在有限状态MDP上获得可证的最优性，在连续控制任务上达到接近或匹配教师的表现。这为可解释强化学习提供了一条新路径：从黑盒到可读、可验证、可迭代的逻辑程序。

## 2. 方法论

- **核心思想**：三阶段管道将冻结的PPO教师转化为Prolog程序，再通过精确回报爬山编辑规则以超越教师。
- **关键技术细节**：
  - **阶段1：提取（Extract）**：在模型可访问下枚举所有可达状态集，查询教师的贪心动作（argmax action），得到完整标签和教师折扣访问分布。
  - **阶段2：归纳（Induce）**：采用FOIL风格的有序规则列表归纳算法，贪心地生长子句。每个子句体是谓词词汇（例如方向、相邻等）的合取，头是动作变量。最终以默认子句闭合，保证全确定性。输出为SWI-Prolog可执行程序，包含感知层、路径搜索和决策列表。
  - **阶段3：扩展（Expand）**：对规则基进行爬山编辑（添加子句、重排序、剪枝），每次编辑仅当精确回报（通过求解Bellman线性系统）增加至少τ时接受。由于回报可精确计算，编辑决策无蒙特卡洛噪声。
- **理论保证（四大定理/命题）**：
  - **定理1（回报损失界）**：对于确定性学生策略πS，有 J(π̄_T) - J(π_S) ≤ 2R_max·ϵ†/(1-γ)²，其中ϵ†是学生与贪心教师的加权不一致率。该界在有限MDP中是可计算验证的。
  - **命题1（单调改进与终止）**：扩展循环满足接受编辑后回报单调增加至少τ，总接受次数≤ 2R_max/((1-γ)τ)，故在有限次调用后终止于τ-局部最优。
  - **命题2（精确计算与机器可验证证书）**：在已知模型下，通过求解线性系统（I-γPπ）v=rπ 可精确得到J(π)、dπ等，从而定理1的左右两边均可精确计算，成为机器可验证的证书。
  - **定理2（连续观测下阈值转换的一致性）**：对于紧致观测空间X⊂ℝᵈ，用B分位网格构建命题阈值决策列表类L_B，最优不一致率ϵ†_B ≤ Cρ_max P/B（O(1/B)），且当B→∞时ϵ†_B→0。
  - **命题3（必要性及维数灾难）**：对于均匀分布X=[0,1]ᵈ上的斜半空间教师，任何L_B中的学生都有ϵ†≥c/B，且需要Ω(Bᵈ⁻¹)个网格单元覆盖边界，即达到精度ϵ需要指数级（1/ϵ）ᵈ⁻¹个规则。

## 3. 实验设计

- **环境/场景**：
  - **KeyDoor**（离散确定性网格世界）：4行×5列，两室中间有门，需拾取钥匙、开门、到达目标。总共16944个可达状态。奖励：到达目标+1，每步-0.01，γ=0.99，最大步长120。
  - **连续控制任务**（Gymnasium）：LunarLander（8维观测，4动作）、CartPole（4维观测，2动作）、Acrobot（6维观测，3动作）。
- **基准（Benchmark）**：教师是从冻结的PPO策略，其中贪心策略（argmax teacher）是蒸馏目标。
- **对比方法**：
  - **Naive蒸馏**（一次性行为克隆）：仅从教师访问分布采样训练，无DAGGER。
  - **DAGGER蒸馏**（本文方法）：使用DAGGER循环，学生自身被回滚，教师重新标记，联合训练。
  - **教师本身**：随机教师和贪心教师。
- **评价指标**：精确回报（离散问题通过线性系统求解）、蒙特卡洛回报（连续任务，共享种子）、成功率、不一致率（保真度）、子句数。统计使用四分位均值（IQM）和分层bootstrap置信区间，以及Wilcoxon符号秩检验（含Bonferroni校正）。

## 4. 资源与算力

- 文中未明确说明使用的GPU型号、数量及训练时长。PPO训练细节：两层tanh激活的actor-critic网络，8个同步环境，标准超参数。收敛教师训练至多800k环境步（当贪心策略成功率达到0.95时提前停止），预算限制教师固定299,008步。未提及硬件平台或集群信息。因此无法量化具体算力开销。

## 5. 实验数量与充分性

- **KeyDoor**：两个教师设置（R1收敛，R2预算限制），每个设置10个随机种子，共20条学习轨迹。每个种子下评估教师、蒸馏学生和扩展学生（精确回报）。展示了精确上限（所有种子扩展均达到最优0.812204）。统计比较：配对检验，含Bonferroni校正（6个检验）。
- **连续控制**：三个任务，每个任务5个PPO教师种子，分辨率B∈{2,3,4,6,8,12}扫描，因此每个任务约30个蒸馏实验。分别比较Naive和DAGGER。还提供了合成边界实验（2/3/4维）验证理论O(1/B)和Ω(Bᵈ⁻¹)（在分辨率验证脚本中）。
- **充分性**：实验覆盖离散精确验证和连续实际评估；对比了naive与DAGGER；分辨率扫描验证了理论趋势；提供了统计显著性和置信区间，并诚实指出多重校正后的非显著及证书的空洞性。总体设计客观、公平。
- **局限**：连续任务种子数较少（5个），可能导致某些比较（如蒸馏vs扩展后的改进）未达到Bonferroni校正后的显著性。LunarLander未完全解决（回报未达教师），符合理论下界预期。

## 6. 主要结论与发现

- **KeyDoor离散环境**：扩展后的Prolog程序在所有种子下达到精确最优回报和成功率1.0。在预算限制设置下，10个种子全部超越随机教师，平均配对回报改善0.100（95% CI [0.096, 0.104]），Wilcoxon p=0.002（Bonferroni校正后p=0.0117）。扩展循环单调提升，每个种子至多两次编辑即达最优。
- **连续控制**：DAGGER蒸馏程序在CartPole上恢复约97%的教师回报（B=3时约482 vs 教师496），在Acrobot上B=8时匹配教师（噪声范围内），在LunarLander上仅部分恢复（B=12时+155 vs 教师+221），符合指数下界预测（8维观测导致天花板）。
- **保真度与子句数**：保真度随分辨率单调增加，LunarLander在B=12时约0.78；子句数随分辨率增长（从约9到约300）。B是状态划分精细度与可读性之间的交换参数。
- **证书**：定理1的证书在所有40个记录中成立，但γ=0.99时右端约10⁴而值域直径仅198，故证书空洞（vacuous）。但所有学生均确切超越贪心教师（measured gap为负）。
- **理论验证**：合成斜边界实验拟合的log-log斜率验证了O(1/B)和Ω(Bᵈ⁻¹)。

## 7. 优点

- **创新性**：首次将事后蒸馏与Prolog执行程序结合，并引入基于精确回报的扩展编辑机制，使符号学生能够超越教师（而不仅是模仿）。
- **理论完备**：提供四种严格的保证（回报损失界、单调改进与终止、连续观测一致性、及维数灾难下界），且有限MDP下证书可机械验证。
- **实验设计严谨**：离散环境精确求解所有量（无蒙特卡洛噪声）；连续任务使用DAGGER、共享种子、IQM+bootstrap统计，诚实报告非显著性结果和证书空洞。
- **可读性**：提供了真实Prolog代码（Listing 1 & 2），验证了程序的实用性和可理解性。
- **通用性**：管道可扩展到连续观测（通过分位网格），且DAgger循环有效缓解分布漂移。

## 8. 不足与局限

- **规模限制**：离散任务需要精确模型和枚举状态（可达16944），代价为每线性求解O(n³)；连续任务依赖DAgger和蒙特卡洛评估，引入置信参数，且不能保证精确最优。
- **谓词词汇手工设计**：方向、相邻等谓词需人工定义；程序不能自动发明词汇（谓词发明缺失），依赖任务知识。作者指出可结合LLM或目标中心提取弥补。
- **证书空洞**：在γ=0.99时，定理1的界过于宽松（约10⁴），实际价值有限；需要更紧的界或更小的有效折扣因子才能实用。
- **统计显著性问题**：KeyDoor中蒸馏vs扩展的改进在Bonferroni校正后未达显著（p=0.0312，校正后p>0.05），作者诚实报告不能声称显著分离。连续任务种子数仅5个，限制了统计功效。
- **LunarLander未完全解决**：部分恢复的结果符合下界，但意味方法在精细连续控制任务上仍有根本性局限，需要更高分辨率或更智能的规则表示。
- **资源信息缺失**：未报告GPU型号、训练时长等，不利于复现和评估成本。

（完）
