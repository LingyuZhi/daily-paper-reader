---
title: "CompactionRL: Reinforcement Learning with Context Compaction for Long-Horizon Agents"
title_zh: CompactionRL：面向长程智能体的上下文压缩强化学习
authors: "Yujiang Li, Zhenyu Hou, Yi Jing, Jie Tang, Yuxiao Dong"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05378v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 提出CompactionRL，一种面向长程自主LLM智能体的上下文压缩强化学习策略
tldr: "长程LLM代理受限于有限上下文窗口，上下文压缩虽可缓解但未与强化学习结合。本文提出CompactionRL，联合优化任务执行和摘要生成，采用token级损失归一化和跨轨迹广义优势估计。在编程任务上，GLM-4.5-Air模型SWE-bench Verified Pass@1达66.8%，Terminal-Bench 2.0达24.5%，分别提升7.0和3.1点。首次将压缩与RL结合训练长程代理，已部署于GLM-5.2训练。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05378v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1387, \"height\": 543, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1416, \"height\": 724, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05378v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1421, \"height\": 414, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05378v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1280, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05378v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1459, \"height\": 900, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05378v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1441, \"height\": 757, \"label\": \"Table\"}]"
motivation: 长程LLM代理因有限上下文窗口导致任务中断，已有压缩方案未与强化学习结合，需联合训练方法。
method: CompactionRL联合优化任务执行与摘要生成，采用token级损失归一化和跨轨迹广义优势估计。
result: "GLM-4.5-Air在SWE-bench Verified和Terminal-Bench 2.0上Pass@1分别提升7.0和3.1点，GLM-4.7-Flash分别提升5.5和6.8点。"
conclusion: CompactionRL有效训练长程代理，已用于GLM-5.2的强化学习管线中。
---

## 摘要
长程智能体LLM越来越受限于有限上下文窗口，因为扩展的交互轨迹可能在任务完成前超出最大上下文长度。上下文压缩通过总结先前的交互状态并在压缩上下文中继续滚动提供了一种自然的解决方案，但将压缩融入强化学习仍未被充分探索。我们提出CompactionRL，一种利用上下文压缩训练长程智能体LLM的强化学习策略。我们的方法通过令牌级损失归一化和跨轨迹广义优势估计联合优化任务执行和摘要生成。该设计使LLM智能体能够从压缩的长程轨迹中学习。我们在开源模型基础上训练CompactionRL，并在智能体编码任务上观察到一致的性能提升。CompactionRL使开源的GLM-4.5-Air模型（106B-A30B）在SWE-bench Verified上达到66.8%的Pass@1得分，在Terminal-Bench 2.0上达到24.5%，分别绝对提升7.0和3.1个百分点。基于GLM-4.7-Flash（30B-A3B），CompactionRL将Pass@1分别提升5.5和6.8个百分点，在SWE-bench Verified上达到56.0%，在Terminal-Bench 2.0上达到20.2%。因此，CompactionRL被部署在训练开源GLM-5.2模型（750B-A40B）的RL流程中。

## Abstract
Long-horizon agentic LLMs are increasingly limited by finite context windows, as extended interaction trajectories can exceed the maximum context length before a task is completed. Context compaction offers a natural solution by summarizing previous interaction states and continuing the rollout under a compressed context, but incorporating compaction into reinforcement learning remains underexplored. We propose CompactionRL, a reinforcement learning strategy to train long-horizon agentic LLMs with context compaction. Our approach jointly optimizes task execution and summary generation with token-level loss normalization and cross-trajectory generalized advantage estimation. This design enables the LLM agents to learn from compacted long-horizon trajectories. We train CompactionRL on top of open models and observe consistent performance gains on agentic coding tasks. CompactionRL enables the open GLM-4.5-Air model (106B-A30B) to achieve Pass@1 scores of 66.8% on SWE-bench Verified and 24.5% on Terminal-Bench 2.0, with absolute gains of 7.0 and 3.1 points, respectively. Built upon GLM-4.7-Flash (30B-A3B), CompactionRL improves Pass@1 by 5.5 and 6.8 points, reaching 56.0% on SWE-bench Verified and 20.2% on Terminal-Bench 2.0, respectively. CompactionRL is thus deployed in the RL pipeline for training the open GLM-5.2 model (750B-A40B).

---

## 论文详细总结（自动生成）

# CompactionRL：面向长程智能体的上下文压缩强化学习 - 详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：在大模型作为智能体执行长程交互任务（如软件工程、终端问题解决）时，随着交互历史累积，令牌序列可能超过模型的固定上下文窗口，导致任务中断。已有**上下文压缩**（将早期历史总结为短摘要后继续交互）被作为推理时启发式使用，但**从未被整合进强化学习训练**。这导致压缩质量未端到端优化，限制了长程智能体的有效训练。

- **整体含义**：论文提出**CompactionRL**——一种基于PPO的强化学习框架，将**上下文压缩作为可训练的行为**融入智能体的rollout收集和优化中，使模型学会在固定上下文预算下高效地执行长期任务。该工作首次系统地将压缩与RL结合，显著提升了开源模型在代码智能体基准上的性能，并已部署于GLM-5.2的训练流程。

## 2. 论文提出的方法论

### 核心思想
- 将上下文压缩作为rollout收集的一部分：当剩余上下文预算低于阈值 `T_comp` 时，策略生成一个摘要，然后从摘要和最近几轮交互重建上下文继续执行。
- 摘要令牌和执行令牌来自**同一个可训练策略**，共享最终任务奖励，从而联合优化任务执行和摘要生成。
- 解决压缩带来的训练问题：
  - **不适用组归一化方法**（如GRPO）：因为压缩导致 rollout 被分成可变数量的段，且同一rollout的段共享相同奖励，组归一化会导致偏差。
  - 因此采用**PPO**，利用值函数做优势估计。

### 关键技术细节
1. **令牌级损失归一化**：所有可训练令牌（包括执行令牌和摘要令牌）在损失中具有相同权重，避免因段数量或长度不均导致的加权偏差。
2. **跨轨迹广义优势估计（Cross-Trajectory GAE）**：对每个段独立的GAE优势乘上校正因子 `(γλ)^(N_>s)`，其中 `N_>s` 是该段之后后续段的可训练令牌总数，从而使得早期段的奖励折扣距离与原始长轨迹一致。
3. **训练流程**：使用PPO，配合值函数预训练、长度自适应GAE（λ=1-1/(αl)，α=1.5）。每个批次进行两次值模型更新、一次策略更新。压缩触发阈值设为10,240个token，最多3次压缩。

## 3. 实验设计

### 数据集与场景
- **SWE-bench Verified**：从完整基准随机抽取200个实例；评估Pass@1（%）。
- **Terminal-Bench 2.0**：完整集；评估Pass@1（%）。
- 使用Harbor环境 + Terminus-KIRA智能体框架，top-p=1.0，温度=1.0，最多250交互轮次。

### 对比方法
- **基模型**：GLM-4.7-Flash（30B-A3B）和 GLM-4.5-Air-SFT（106B-A30B）
- **标准RL无压缩**：PPO训练但不使用压缩（上下文预算64k/80k）
- **CompactionRL**：本文方法，使用压缩训练
- **消融变体**：
  - 无摘要训练（启用压缩但不优化摘要令牌）
  - 无令牌级损失、无跨轨迹GAE
- **公共参考**：GPT-5 mini、Qwen3-Coder、gpt-oss-120b等（仅参考，因agent scaffold可能不同）

### 评估设置
- 单窗口评估（×1）：不使用压缩，峰值长度64k/80k
- 压缩评估（×4）：允许最多3次压缩（有效预算4倍峰值长度）
- 长窗口参考评估（Long）：使用更大非压缩上下文（128k/160k）

## 4. 资源与算力

- **训练框架**：使用开源异步RL框架 slime。
- **未明确说明的算力**：
  - 论文未提供具体GPU型号、数量、训练时长。
  - 给出超参数：全局batch size=128，组大小=1，Adam学习率策略2e-6，值模型3e-6。
  - 训练数据来自 SWE-Dev（开源数据集）。
  - 值模型预训练50步后才开始RL。
- **评价**：算力信息缺失，对于大规模模型（106B、30B）的训练成本缺乏透明度。

## 5. 实验数量与充分性

### 实验组数
- **主结果表2**：2个模型 × 3种训练方式（基模型、RL无压缩、CompactionRL）× 2个基准 × 2种评估设置 → 共24个数据点（部分展示均值）。
- **消融表3**：7种系统配置 × 2个模型规模 × 2个基准 × 3种评估设置 → 大量对比。
- **消融表4**：4种系统配置 × 2个基准 → 验证令牌级损失和跨轨迹GAE必要性。
- **行为分析图3**：压缩次数、工具调用、压缩触发任务准确率。
- **训练动态图4**：摘要长度、推理令牌、策略熵。

### 实验充分性评价
- **充分**：覆盖了两个不同规模的模型、两个代表性长程编码基准、多种训练变体、两种评估设置。消融实验针对关键组件（摘要训练、令牌级损失、GAE）进行了分离研究。
- **客观**：使用公开基准和固定seed，报告均值（2次运行）。公共参考数据来自模型卡片，但直接对比受scaffold差异影响，作者已注明。
- **局限性**：
  - 仅限代码相关任务，未涉足Web交互、具身智能等更广域智能体。
  - 单窗口评估下CompactionRL并未一致提升，存在训练-测试不匹配。
  - 跨轨迹GAE仍是近似，不能完全捕捉多段压缩的因果效应。
  - 未做噪声敏感性或超参数扫描。

## 6. 论文的主要结论与发现

1. **CompactionRL在固定峰值上下文预算下显著优于基模型和标准RL**：
   - GLM-4.5-Air：SWE-bench Verified 66.8%（+7.0），Terminal-Bench 2.0 24.5%（+3.1）。
   - GLM-4.7-Flash：SWE-bench Verified 56.0%（+5.5），Terminal-Bench 2.0 20.2%（+6.8）。
2. **摘要训练是性能提升的关键**：不优化摘要令牌的变体（CompRL w/o sum.）性能明显下降，且摘要长度随训练增长（更详细），推理令牌也增加。
3. **令牌级损失归一化和跨轨迹GAE均不可或缺**：移除任一会导致性能退化（表4）。
4. **压缩行为改善**：CompactionRL使用更少的压缩次数和工具调用，且在需要压缩的任务上准确率最高，表明其学习了更有效的压缩策略。

## 7. 优点

### 方法亮点
- **首次将上下文压缩作为可训练的行为融入RL**，而非推理时启发式，实现了端到端优化。
- **针对压缩导致的问题提出了两个有效技术**：令牌级损失归一化和跨轨迹GAE，理论正确且实验验证有效。
- **联合优化执行与摘要**：摘要生成不是外部模块，而是策略的一部分，使得摘要质量直接影响最终奖励。

### 实验设计亮点
- 在两种不同规模的开源模型上验证（30B和106B），并展示了向更大模型（GLM-5.2）的部署可能性。
- 消融实验分离了压缩启用、摘要训练、损失归一化、GAE等贡献，分析清晰。
- 提供了训练动态分析，有助于理解摘要行为和学习过程。

## 8. 不足与局限

1. **训练-测试不匹配**：CompactionRL在单窗口（无压缩）评估下未一致提升，甚至略有下降，说明模型过度适应了压缩环境，限制了在无压缩场景的直接使用。
2. **信用分配近似**：跨轨迹GAE使用指数折扣因子的乘积近似，可能无法完全准确反映早期摘要对最终结果的真实因果影响，尤其当存在多次压缩时。
3. **领域覆盖窄**：实验仅聚焦于代码相关的长程任务（SWE-bench、Terminal-Bench），未验证Web智能体、对话智能体等更广泛场景下的有效性。
4. **计算成本不透明**：未报告训练所需GPU数量、时长，也未讨论压缩训练相比标准RL的额外开销（例如摘要生成前向计算）。
5. **评估局限性**：SWE-bench仅使用200个子集，可能降低统计稳定性；Terminal-Bench 2.0评估未说明是全部还是子集（正文写“full set”）。公共参考基准的scaffold差异使得横向对比不完全公平。
6. **压缩次数限制**：最多3次压缩，可能不足以覆盖极长任务；更灵活的压缩策略（如自适应次数）未探索。

（完）
