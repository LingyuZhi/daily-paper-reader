---
title: "A Control System, a Dataset, and a Recipe for Making Frozen LLM Agents Learn a Domain"
title_zh: 一个控制系统、一个数据集以及一份使冻结的LLM代理学习领域的配方
authors: Debjyoti Paul
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25415v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 利用强化学习优化冻结LLM智能体的控制系统动作空间
tldr: 生产级LLM代理通常由冻住模型和外挂组件(harness)构成，但现有优化方法成本高或不可审计。本文提出将harness视为小型可读动作空间，使用在线强化学习(ε-贪心上下文赌博机与REINFORCE)学习策略，以多目标奖励(任务成功、验证得分等)优化。在工具使用、代码生成(HumanEval)和多跳问答(HotpotQA)三个任务及Ollama/AWS Bedrock两个平台上，该方法优于静态基线。相关代码、数据集和部署方案已开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25415v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 998, \"height\": 819, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25415v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1537, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25415v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1561, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25415v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1177, \"height\": 266, \"label\": \"Table\"}]"
motivation: 冻住LLM代理的harness优化缺乏高效、可审计的方法，现有方案需昂贵搜索或不可控自修改。
method: 将harness视为固定可读动作空间，用ε-贪心上下文赌博机和REINFORCE在线学习策略，评分基于多目标奖励。
result: 在三个可验证任务和两个模型平台上，该方法均优于DSPy静态基线，并开源了完整代码和日志。
conclusion: 一种轻量级、可审计的harness控制方案，附带跨域验证套件和部署模板，易于迁移至新领域。
---

## 摘要
生产级LLM代理越来越多地由一个封装在套件中的冻结模型组装而成：一个提示模板、一个工具集、一个记忆/检索层、一个规划策略和一个验证策略。两个2026年的系统，Meta-Harness (Lee et al., 2026)和HyperAgents (Meta AI, 2026)表明，这个套件本身可以由一个代理提议者进行优化甚至自我重写——但代价要么是昂贵的代码搜索循环，要么是无约束的自我修改代码，这两者都无法审计，也无法与完全黑盒模型API一起使用。我们采取了一个更狭窄、更受约束的立场：将套件视为一个小的、固定的、人类可读的动作空间，并使用经典的样本高效强化学习（一个ε-贪婪上下文赌博机和REINFORCE）在线学习其上的策略，根据多目标奖励（任务成功、验证器分数、策略合规性、成本、延迟和未支持声明惩罚）进行评分。我们使用DSPy (Khattab et al., 2024)作为上下文组装器和最强非自适应基线（DSPy BootstrapFewShot静态提示）的来源来实例化这个控制系统，并在三个可验证任务领域——工具使用工作流、代码生成（HumanEval）和多跳检索问答（HotpotQA）——以及两个模型提供商（本地Ollama模型和AWS Bedrock）上进行评估。我们发布了套件控制系统代码、跨领域可验证任务套件、训练期间的完整轨迹/奖励分解日志，以及一个独立于提供商的部署配方，用于将此应用于新组织的领域和验证设置。

## Abstract
Production LLM agents are increasingly assembled from a frozen model wrapped in a harness: a prompt template, a tool set, a memory/retrieval layer, a planning strategy, and a verification policy. Two 2026 systems, Meta-Harness (Lee et al., 2026) and HyperAgents (Meta AI, 2026), show that this harness can itself be optimized or even self-rewritten by an agentic proposer -- at the cost of either an expensive code-search loop or unconstrained self-modifying code, neither of which is auditable or usable with a fully black-box model API. We take a narrower, more constrained position: treat the harness as a small, fixed, human-legible action space and learn a policy over it online with classic sample-efficient reinforcement learning (an $ε$-greedy contextual bandit and REINFORCE), scored against a multi-objective reward (task success, verifier score, policy compliance, cost, latency, and an unsupported-claim penalty). We instantiate this control system with DSPy (Khattab et al., 2024) as both the context assembler and the source of the strongest non-adaptive baseline (a DSPy BootstrapFewShot static prompt), and evaluate it across three verifiable task domains -- tool-use workflows, code generation (HumanEval), and multi-hop retrieval QA (HotpotQA) -- and two model providers (a local Ollama model and AWS Bedrock). We release the harness-control-system code, the cross-domain verifiable task suite, the full trajectory/reward-decomposition logs from training, and a provider-agnostic deployment recipe for applying this to a new organization's domain and verification setup.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

生产级LLM代理通常由冻结的基座模型 + 外挂组件（harness，包括提示模板、工具集、记忆/检索层、规划策略、验证策略）构成。现有工作（如Meta-Harness、HyperAgents）尝试让LLM自动重写整个harness代码，但存在**成本高、难以审计、依赖可编程环境**等问题，无法适用于黑盒API或防火墙后的常见生产场景。本文提出一种更受约束的方案：将harness视为一个小型、固定、人类可读的离散动作空间，通过经典强化学习（ε-贪心上下文赌博机和REINFORCE）在线学习最优策略，目标是**在保证可审计、低成本的前提下，使冻结的LLM代理适应特定领域**。

## 2. 方法论

- **核心思想**：将harness的配置分解为6个离散杠杆（prompt风格、工具/检索策略、记忆策略、规划策略、验证策略、最大步数），共3^5×3=729种配置，构成一个固定、可枚举的动作空间。使用在线强化学习（上下文赌博机或REINFORCE）学习一个策略，以多目标奖励（任务成功、验证器得分、策略合规性、成本、延迟、未支持声明惩罚）为优化目标。
- **关键技术细节**：
  - 使用DSPy作为上下文组装器，根据选择的配置构建DSPy Signature和Module。
  - 奖励函数：R = w_s·success + w_v·verifier_score + w_c·compliance − w_u·unsupported_claims − w_$·tokens/1000 − w_ℓ·latency/1000。
  - 控制器：随机（下限）、ε-贪心上下文赌博机（线性值估计、乐观初始化）、REINFORCE（softmax策略、熵正则化、移动平均基线）。
  - 对比基线：DSPy-static（通过DSPy BootstrapFewShot优化的静态提示）。
- **算法流程**（文字描述）：
  1. 定义任务分布和特征向量。
  2. 定义确定性验证器。
  3. 定义小且可枚举的harness动作空间。
  4. 使用提供商中立的LLM适配器。
  5. 先用DSPy优化静态基线（day zero）。
  6. 部署在线控制器，记录完整轨迹。
  7. 低置信度时升级到人类（agentic-RPA循环）。
  8. 监控奖励分解作为审计轨迹。
  9. 离线回放检查漂移。
  10. 添加安全约束。

## 3. 实验设计

- **数据集/场景**：三个可验证任务领域
  - 工具使用：40个合成多工具CRM工作流（状态diff验证）。
  - 代码生成：40个HumanEval问题（单元测试执行验证）。
  - 多跳检索问答：40个HotpotQA问题（精确匹配+token-F1验证）。
  总共120个任务（96训练/24测试，80/20划分）。
- **基准**：对比方法包括随机搜索、DSPy静态基线（最强非自适应基线）、ε-贪心上下文赌博机、REINFORCE。
- **模型提供商**：Ollama（本地，qwen2.5:7b）和AWS Bedrock（Haiku）；另对Bedrock Sonnet进行了强模型点检查。
- **评估指标**：主要报告成功率（正确率）和平均token数/episode。

## 4. 资源与算力

文中未明确提及GPU型号、数量或训练时长。但提到：
- 实验总花费为$7.62。
- Ollama串行处理约2.7 episodes/分钟，Bedrock Haiku支持并发。
- 主矩阵共2,460 episodes，额外样本效率实验1,200 episodes，强模型点检查960 episodes。
- 推测使用少量CPU/GPU资源，但未具体说明硬件配置。

## 5. 实验数量与充分性

- **实验数量**：
  - 主矩阵：4种优化器 × 3个领域 × 2个模型 = 24个条件，每个条件1-3个种子，共2,460 episodes。
  - 样本效率实验：仅在工具使用×Bedrock Haiku上运行300 episodes/run×2种子（1,200 episodes）。
  - 强模型点检查：Bedrock Sonnet上2种子×40 episodes×3领域×4优化器（960 episodes）。
  - 总计约4,620 logged episodes（文中说“4,620 logged episodes”）。
- **充分性**：作者承认实验规模有限——“最多3个种子、25-60个episodes/run，远低于动作空间大小（729）”。样本效率实验表明在线控制器需要远超当前预算才能达到极限。因此，实验覆盖范围偏小，但作者透明地指出了这一点，并提供了额外长epoch实验佐证。

## 6. 主要结论与发现

- **意外结果**：DSPy优化的静态基线在所有领域和模型上均匹配或优于在线控制器（bandit和REINFORCE），且通常token成本更低（如工具使用Bedrock Haiku：静态96%成功率@285 tokens vs. REINFORCE 62%@680 tokens）。
- **原因分析**：这并非性能天花板，而是**样本效率差距**：动作空间729种配置，而在线控制器仅分配25-60 episodes，远低于Majumdar (2026)指出的Ω(M)样本量要求。300 episodes的长实验显示在线控制器仅缓慢爬升，仍未追上静态基线。
- **实践指导**：对于实际部署，应先用DSPy优化静态基线（冷启动），再考虑在线控制器（任务分布漂移时）。在线控制器不应从均匀先验开始。

## 7. 优点

- **可审计性**：每个决策对应一个命名杠杆，回归可归因于具体配置，而非代码diff。
- **轻量级**：不需要代码执行权限，适用于黑盒API。
- **多目标奖励**：将合规性、未支持声明等作为一等信号，而不仅仅是任务成功。
- **开源完整成果**：包括控制系统代码、120个任务数据集、4,620条完整轨迹日志、部署配方(RECIPE.md)。
- **暴露和修复实践陷阱**：发现了冷启动问题（贪心策略陷入第0个动作）和崩溃隔离问题（单次失败导致整个作业崩掉），并给出了修复方法，对工程实践有参考价值。

## 8. 不足与局限

- **实验规模小**：种子数最多3个，每个条件仅25-60 episodes（主矩阵），远低于动作空间大小，统计效能有限。
- **样本效率差距**：在线控制器在给定预算下无法收敛到DSPy静态基线的水平，但作者仅分析了原因，未提出有效解决方案（仅建议未来工作用静态基线初始化）。
- **OpenAI API不可用**：原计划作为第三提供商，但密钥不可用，导致矩阵不完整。
- **工具使用领域合成**：使用mock CRM数据集，非真实生产系统。
- **未支持声明检测启发式**：仅适用于检索领域的词法重叠，非通用幻觉检测器。
- **未设置全局支出上限**：虽然单次运行有上限，但整体研究超支（$7.62 > $5预跑上限），作者指出这是“自身harness的缺口”。
- **缺乏消融实验**：未分析各杠杆的重要性，未比较不同奖励权重的影响。
- **未探索初始化策略**：正如结论所述，从静态基线初始化在线控制器是自然下一步，但未在本文中实现。

（完）
