---
title: Single-Rollout Asynchronous Optimization for Agentic Reinforcement Learning
title_zh: 单次回滚异步优化用于智能体强化学习
authors: "Zhenyu Hou, Yujiang Li, Jie Tang, Yuxiao Dong"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07508v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 用于大模型后训练的智能体强化学习异步优化
tldr: 异步强化学习在LLM后训练中效率高但存在稳定性和策略陈旧问题。本文提出单 rollout 异步优化（SAO），用单 rollout 替代分组采样并引入严格的双面 token 级裁剪。SAO 在 SWE-Bench Verified 等基准上持续优于 GRPO 及其变体，并在模拟在线学习中表现有效。该工作成功部署于 750B 参数 GLM-5.2 模型的 agentic 训练中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07508v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1342, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1011, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 594, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1420, \"height\": 414, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1422, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 644, \"height\": 426, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07508v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1396, \"height\": 655, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 575, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1420, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 761, \"height\": 218, \"label\": \"Table\"}]"
motivation: 现有异步 RL 系统侧重吞吐量，忽视训练稳定性和任务效果，且 GRPO 的分组采样不适应异步 agentic 训练。
method: 采用单 rollout 采样降低策略陈旧风险，结合实用价值模型训练，并引入严格双面 token 级裁剪提升优化稳定性。
result: SAO 在 agentic 编码和推理基准上持续超越 GRPO 及其变体，且能稳定训练一千步，在模拟在线学习中表现优越。
conclusion: SAO 有效解决了异步 RL 的稳定性与策略陈旧问题，成功应用于大规模 LLM 的 agentic 训练。
---

## 摘要
强化学习在大型语言模型的后训练中变得越来越重要。先前的LLM强化学习流水线大多是同步和批量交错的，这对于长周期智能体任务效率较低。最近，异步强化学习通过随着回滚到达更新模型，成为一种更高效的替代方案。然而，现有的异步强化学习系统往往强调吞吐量，而对训练稳定性和任务有效性探索不足。例如，一个关键挑战是广泛使用的GRPO框架中的分组采样并不自然地适应异步智能体训练。在本文中，我们提出了单次回滚异步优化（SAO）来解决异步强化学习中的稳定性和离策略挑战。为了减少离策略影响并提高泛化能力，我们用单次回滚采样替换分组采样，即每个提示使用一次回滚。我们通过实用的价值模型训练设计进一步改进了这种单次回滚策略。为了提高优化稳定性，我们引入了严格的双侧词元级别裁剪策略。SAO能够稳定训练一千步，并在智能体编码和推理基准测试（如SWE-Bench Verified、BeyondAIME和IMOAnswerBench）上持续优于GRPO及其变体。我们还证明了单次回滚强化学习在模拟在线学习设置中特别有效，其中模型必须适应不断变化的环境。为此，SAO已成功部署在智能体强化学习流水线中，用于训练开放的GLM-5.2模型（750B-A40B）。

## Abstract
Reinforcement learning (RL) is becoming increasingly important for post-training large language models (LLMs). Previous RL pipelines for LLMs were mostly synchronous and batch-interleaved, which is inefficient for long-horizon agentic tasks. Recently, asynchronous RL has emerged as a more efficient alternative by updating the model as rollouts arrive. However, existing asynchronous RL systems often emphasize throughput, while leaving training stability and task effectiveness largely underexplored. For example, a key challenge is that group-wise sampling in the widely-used GRPO framework does not naturally fit asynchronous agentic training. In this paper, we present Single-rollout Asynchronous Optimization (SAO) to address the stability and off-policy challenges in asynchronous RL. To reduce off-policy effects and improve generalization, we replace group-wise sampling with single-rollout sampling, that is, using one rollout per prompt. We further improve this single-rollout strategy with practical value-model training designs. To improve optimization stability, we introduce a strict double-side token-level clipping strategy. SAO is able to train stably for one thousand steps and consistently outperform GRPO and its variants on agentic coding and reasoning benchmarks, such as SWE-Bench Verified, BeyondAIME, and IMOAnswerBench. We also demonstrate that single-rollout RL is particularly effective in a simulated online learning setting, where the model must adapt to changing evolving environments. To this end, SAO is successfully deployed in the agentic RL pipeline for training the open GLM-5.2 model (750B-A40B).

---

## 论文详细总结（自动生成）

好的，以下是对给定论文《Single-Rollout Asynchronous Optimization for Agentic Reinforcement Learning》的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **研究动机**：
    *   大型语言模型（LLM）的后训练中，强化学习（RL）变得日益重要。
    *   现有的LLM强化学习流水线大多是同步和批量交错的，即等待一个批次的所有轨迹（rollout）生成完毕后才开始训练。这在处理长周期、长度可变的智能体（agentic）任务（如编程）时效率低下，因为短轨迹完成后必须等待长轨迹，导致大量GPU资源闲置。
    *   异步强化学习（Async RL）作为一种更高效的替代方案被提出，它允许在轨迹到达后立即进行训练，从而提高硬件利用率。

*   **核心挑战**：
    *   **训练稳定性**：异步训练引入了“策略滞后”（policy lag），即用于生成轨迹的模型版本可能与当前训练模型不同，这会导致严重的离策略（off-policy）问题，损害训练稳定性。
    *   **任务有效性**：现有的异步RL系统主要关注吞吐量优化，而对稳定性和最终任务性能探索不足。
    *   **与GRPO不兼容**：当前流行的GRPO方法依赖于同一提示下的**分组采样**（group-wise sampling）进行优势估计。这种方法在异步训练中存在天然缺陷：
        1.  必须等待组内最慢的轨迹完成才能开始训练，引入了延迟。
        2.  在动态、复杂的在线环境中，每个提示通常只有一个反馈，分组采样不再适用。

*   **整体含义**：
    *   论文提出单次回滚异步优化（SAO），旨在解决异步强化学习中的稳定性和离策略问题，使其在保持异步高效性的同时，能够稳定、有效地用于智能体任务。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想**：用**单次回滚采样**（single-rollout sampling）替代GRPO的**分组采样**，并结合一系列关键技术来降低方差、提升稳定性和处理智能体任务特有的挑战。

*   **关键技术细节**：

    1.  **稳定异步RL：直接双面重要性采样**
        *   **问题**：异步训练中，跟踪生成轨迹的确切旧策略（old policy）模型版本成本过高。
        *   **方案**：
            *   直接使用**当前策略**（πθ）对的比率和**回滚策略**（π_rollout）的概率，即 `r_t(θ) = πθ / π_rollout`，从而省略对潜在过时的 `π_old` 的追踪。
            *   采用**严格的双面token级别裁剪与掩码**（Double-sided Importance Sampling, DIS）。只有当重要性比率 `r_t(θ)` 落在`[1-ϵ_l, 1+ϵ_h]`区间内时，该token才对梯度计算有贡献。落在区间外的token被完全**掩码**（mask掉），以防止极端的策略偏离。这提供了一种更强的正则化。

    2.  **减少离策略效应：单次回滚与价值模型训练**
        *   **单次回滚**：每个提示只生成一次轨迹，完成后立即用于训练，避免了分组采样的等待和离策略问题，天然适用于在线学习。
        *   **价值模型训练优化**：单次回滚的核心挑战是高方差，因此需要一个高质量的价值模型。
            *   **加快价值更新**（Faster Value Update）：对价值模型的更新频率高于策略模型（例如，策略每更新1次，价值模型更新K=2次），使其能更快地适应新策略。
            *   **冻结注意力层**（Frozen-Attention）：在价值模型训练中，发现全注意力层梯度不稳定。因此，**冻结价值模型的注意力模块参数**，只优化混合专家（MoE）层。这有效稳定了训练。
            *   **跳过观察的token级GAE**（Skip-Observation Token-level GAE）：针对智能体任务（动作-环境反馈-动作）的特性，修改GAE算法。在计算优势时，跳过环境反馈（观察）token，直接计算从一个**动作的最后一个token**到**下一个动作的第一个token**的优势，从而避免了环境反馈噪声对优势估计的干扰。
            *   **扩大价值预训练数据**：通过增加价值预训练的语料规模，解决了价值估计的“冷启动”问题，为后续的单次回滚和快速价值更新提供了良好初始化。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法。

*   **数据集/场景**：
    *   **数学推理基准**：
        *   AIME2025
        *   BeyondAIME
        *   HMMT Nov 2025
        *   IMOAnswerBench
        *   *这些基准均在“使用Python工具进行推理”的设置下评估。*
    *   **智能体编码基准**：
        *   SWE-Bench Verified (使用OpenHands作为框架)
    *   **模拟在线学习任务**：
        *   设计了一个写作风格调整任务，环境反馈的语言风格（如“可爱”、“中二病”、“古典”）会动态变化。

*   **基准方法**：
    *   基线模型：Qwen3-30B-A3B 的SFT版本。
    *   **标准GRPO**（vanilla GRPO）
    *   **GRPO (w/ DIS)**：在GRPO中应用了SAO提出的DIS双面重要性采样策略。
    *   **VAPO**：另一种基于价值的强化学习基线。
    *   **消融实验变体**：如“w/o Faster value”、“w/o Frozen attention”、“Step-level GAE”、“Running-mean baseline”等。

### 4. 资源与算力

*   论文中未明确说明训练所使用GPU的具体型号、数量和训练时长。
*   文中仅提到：
    *   模型规模为Qwen3-30B-A3B（300亿参数，其中30亿是激活的）。
    *   SAO 被成功部署用于训练**GLM-5.2 (750B-A40B)** 模型（7500亿参数，400亿激活），但其训练细节未在本文中详尽描述。
    *   超参数包括：batch size 为 128。

### 5. 实验数量与充分性：大概做了多少组实验，是否充分、客观、公平。

*   **实验组数**：论文进行了**大量且结构化的实验**，包括：
    1.  **主基准测试**：在5个基准（4个数学+1个编码）上对比了SAO、GRPO（有/无DIS）和基线模型，并展示了多轮训练的性能曲线。
    2.  **消融实验**：系统地剥离了SAO的各个组件，包括价值更新频率、注意力冻结、GAE变体（token/step level）、基线选择（vanilla VAPO, running-mean）。
    3.  **模拟在线学习实验**：在动态变化的在线学习场景中比较了SAO和running-mean基线。
    4.  **训练动态分析**：展示了价值模型解释方差、梯度范数、裁剪比例等指标，定量分析了SAO稳定训练的原因。

*   **充分性与公平性**：
    *   **充分**：实验覆盖了从静态基准到动态在线学习的多个场景，消融实验全面，验证了每个设计选择的有效性。
    *   **客观**：通过多次运行取平均（如AIME/HMMT/IMOAnswerBench 16次，BeyondAIME 4次）来降低方差，结果呈现客观。
    *   **公平**：与改进的GRPO (w/ DIS) 变体进行对比，确保比较是基于相同总batch size（128）下的公平对比。消融实验设计清晰，控制变量得当。论文也承认了GRPO在训练初期的表现，点明了SAO的优势在后半段显现。

### 6. 论文的主要结论与发现

1.  **异步RL的稳定性**：SAO提出的**直接双面重要性采样（DIS）** 通过严格的token级裁剪和掩码，有效解决了异步训练中的离策略和稳定性问题，使得训练能稳定进行上千步。
2.  **优于GRPO**：在数学推理和编程基准上，SAO的性能**持续且显著地优于**标准的和改进的GRPO变体。
3.  **单次回滚的优势**：通过**高质量的价值模型**（结合加快更新、冻结注意力、跳过观察GAE等技巧），单次回滚策略是可行且高效的。它不仅在静态基准上表现良好，更是在需要快速适应变化的**模拟在线学习**场景中展现出独特优势，验证了其对动态环境的适应性。
4.  **组件有效性**：消融实验证实了SAO中所有关键组件（更快的价值更新、冻结注意力、skip-observation GAE）对最终性能的提升均是不可或缺的。
5.  **部署验证**：SAO 被成功应用于训练750B参数的大型GLM-5.2模型，证明了其在大规模实践中的可行性。

### 7. 优点：方法或实验设计上有哪些亮点。

*   **方法设计亮点**：
    *   **直面异步RL核心矛盾**：精准地识别并解决了异步训练中“稳定性”和“离策略”这一对核心矛盾。
    *   **对GRPO的根本性改进**：没有被GRPO的成功所局限，而是指出其分组采样的结构性缺陷，并提出单次回滚这一更简洁、更适合异步和在线场景的替代方案。
    *   **实用主义的价值模型设计**：为解决单次回滚的高方差问题，提出了多种务实且有效的技巧（加快更新、冻结注意力），而非单纯追求更复杂的模型。
    *   **智能体任务专属优化**：提出的“跳过观察GAE”是一个针对模型与环境交互轨迹特定结构的巧妙改进，剔除了外部噪声。

*   **实验设计亮点**：
    *   **动态在线学习仿真**：除了静态基准测试，还设计了切换风格的在线写作任务，这一设置非常有力地展示了SAO相比依赖分组信息的GRPO的核心实战优势。
    *   **训练动态深入分析**：不只是报告最终性能，还分析了训练过程中的解释方差、梯度范数、裁剪比例等指标，为算法的稳定性提供了定量证据，使结论更令人信服。
    *   **大规模部署验证**：将SAO成功应用于750B参数级别的模型训练，极大地增强了其方法的说服力和工程实用性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等。

*   **模型规模局限**：核心实验主要在**Qwen3-30B-A3B**上进行。作者的结论可能无法直接迁移到更小或更大的模型，或者其他类型的非数学/推理任务（如对话、摘要等）。
*   **对价值模型依赖**：SAO高度依赖于一个训练良好的价值模型。虽然作者提出了预训练和训练技巧，但这**增加了系统的复杂性和对训练数据的额外需求**。相比无需价值模型的GRPO，其启动门槛更高。
*   **场景局限性**：研究结果主要针对**长周期、奖励稀疏**的智能体任务。对于**短回合、奖励密集**或**非智能体的RLHF**场景，SAO的边际效益可能不那么显著。
*   **在线学习的模拟性质**：其在线学习实验是**模拟**的，使用LLM作为裁判来提供风格偏好奖励。真实的用户在线适应需要更严格的安全措施、监管和隐私审查。
*   **计算资源门槛**：成功大规模部署SAO需要能够可靠地**保存token级行为概率**的异步生成基础设施，这对于一些小型实验室或有资源限制的团队来说可能是一个挑战。
*   **代码与可复现性**：论文未明确提及开源代码或详细的数据处理细节，这可能会增加其他团队复现结果的难度。

（完）
