---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 面向长周期智能体强化学习的组图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向长时域智能体强化学习的组图策略优化
tldr: "长期智能体强化学习面临奖励稀疏和延迟问题，现有步骤级方法信用分配粗粒度且忽略状态转移图结构，导致高方差和局部化分配。本文提出G2PO，将交互轨迹转化为全局状态图，通过分组聚合状态值估计降低方差，并采用边中心优势估计全局标准化TD误差，聚焦关键转换。在WebShop、ALFWorld等基准上，G2PO较GRPO等基线提升最高22.2%，有效解决长期信用分配难题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有方法信用分配粗粒度，将代理探索视为孤立线性轨迹，忽略状态转移图结构，导致高方差值估计和局部化分配。
method: G2PO将交互轨迹转化为全局状态转移图，通过分组聚合状态值估计降低采样方差，并采用边中心优势估计在全局标准化TD误差。
result: "在WebShop、ALFWorld、AppWorld基准上，G2PO较GRPO等SOTA基线提升最高22.2%成功率。"
conclusion: G2PO通过显式利用图结构进行细粒度信用分配，有效克服长期任务中奖励稀疏和延迟问题。
---

## 摘要
基于组的强化学习（RL）在智能体场景中显著增强了大型语言模型（LLMs）。为了实现更细粒度的策略更新，最近的智能体RL框架已从轨迹级训练转向步级训练。然而，长周期智能体RL面临严重的奖励稀疏和延迟问题，因为反馈通常会在数十个交互步骤之后才出现。现有的步级框架虽细化了训练粒度，但其信用分配仍较为粗糙，且仍将智能体探索视为孤立的线性轨迹。这种过度简化的视角忽略了状态转移的固有图结构，导致状态价值估计方差高以及短视、局部的信用分配。为了克服这些关键瓶颈，我们提出了组图策略优化（G2PO），一种专为多轮智能体任务而设计的新型基于组的RL算法。G2PO将线性交互轨迹显式转换为全局状态转移图。通过聚合不同轨迹中的相同观测，我们引入了组聚合状态价值估计，从而降低了采样方差和轨迹依赖偏差。此外，我们将智能体动作重新定义为状态节点之间的转移，并提出了一种基于边的优势估计策略。通过在整个图上全局标准化时间差分（TD）误差，G2PO能够明确识别并优先处理驱动绝对任务进展的关键转移。在代表性长周期基准（WebShop、ALFWorld和AppWorld）上的大量实验表明，G2PO显著优于最先进的基于提示和RL的基线方法，相比于GRPO取得了高达22.2%的成功率提升。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

# 论文结构化总结：Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning (G2PO)

## 1. 核心问题与整体含义（研究动机和背景）
- **研究背景**：大型语言模型（LLMs）在智能体（Agent）场景中已从聊天机器人演进为自主代理，通过多轮交互（如网页购物、物理模拟、软件操作）完成长周期任务。强化学习（RL）已成为提升LLM推理和决策能力的标准后训练方法（如PPO、GRPO、SAPO等）。
- **核心问题**：现有智能体RL方法面临两大瓶颈：
  - **奖励稀疏与延迟**：在长周期任务中，最终奖励往往在数十步后才给出，中间步骤无即时奖励，导致信用分配困难。
  - **状态价值估计方差高**：现有步级（step-level）训练方法将探索视为孤立线性轨迹，忽略状态间的图结构。不同轨迹中相同观测可能通向不同结局，单轨迹回报估计方差大。
  - **局部化信用分配**：如GiGPO等仅在相同状态内比较动作，无法区分微小改进与关键突破性动作的全局重要性。
- **本文目标**：提出G2PO，通过将线性交互轨迹显式转化为全局状态转移图，利用图结构进行细粒度、低方差、全局化的信用分配，提升长周期智能体任务的性能。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将采样的多条轨迹重构为状态-转移图，基于图进行状态价值估计和优势估计。
  - **状态组图构建**：将环境中相同或相似观测（如相同网页内容）聚合成一个状态组节点，动作作为边，构建图 $\mathcal{G}= \{\mathcal{V}, \mathcal{E}\}$。
- **关键技术细节**：
  - **组聚合状态价值估计**：对每个状态组 $G_k$，计算其内所有步骤的折扣回报平均值：$V(G_k) = \frac{1}{|G_k|} \sum_{o_i^j \in G_k} \gamma^{T-j+1} R_i$。此举降低方差，消除单轨迹随机性。
  - **边中心优势估计（Edge-Centric Advantage）**：将动作视为状态转移的边，计算1步TD误差：$\delta_{ij} = V(G_{k'}) - V(G_k)$。然后在全局范围内标准化所有TD误差，得到 $A_{EC}(a_{ij})$。这能识别驱动绝对进展的关键动作。
  - **节点中心优势估计（Node-Centric Advantage）**：在同一个源状态组内，比较不同目标状态组的价值，局部标准化。
  - **回合级优势估计（Episode-Level Advantage）**：保留GRPO风格的基于最终奖励的全局标准化优势。
  - **最终优势**：$A_{ij} = A_{EP}(a_{ij}) + w \cdot (A_{NC}(a_{ij}) + A_{EC}(a_{ij}))$，其中$w$为平衡权重。
  - **优化目标**：类似GRPO的裁剪代理目标加KL散度惩罚，但每个步骤单独计算优势。
- **算法流程**（文字说明）：
  1. 采样$N$条完整轨迹，收集所有观测和动作。
  2. 基于观测相似性划分状态组，构建图。
  3. 计算每个状态组的值（聚合平均）。
  4. 对每个步骤计算三种优势，加权合并。
  5. 使用重要性采样比和裁剪，更新策略参数。
- **公式略**（已在正文展示）。

## 3. 实验设计
- **数据集/场景**：
  - **WebShop**：模拟电商网站，需要搜索、导航、选择商品属性。
  - **ALFWorld**：基于文本的物理仿真环境，六类家务任务（Pick、Look、Clean、Heat、Cool、Pick2）。
  - **AppWorld**：复杂软件生态，通过Python API调用完成多步骤任务。
- **Benchmarks**：这些是公认的长周期、多轮交互智能体基准。
- **对比方法**：三大类共9种基线：
  - **前沿闭源模型**：Qwen3.5-397B、DeepSeek-V3.2、Gemini-2.5-Pro。
  - **提示式方法**：ReAct、Reflexion。
  - **RL训练方法**：PPO（带Critic）、RLOO、GRPO、GiGPO。
- **基础模型**：Qwen2.5-1.5B-Instruct、Qwen2.5-7B-Instruct（主实验），Qwen2.5-14B-Instruct（AppWorld）。

## 4. 资源与算力
- **文中明确说明**：
  - 对于ALFWorld和WebShop，使用4×H100 GPU训练Qwen2.5-1.5B/7B，每组运行100次迭代。
  - 对于AppWorld，使用8×H100 GPU训练Qwen2.5-14B，运行50次迭代。
  - 未提及总训练时长，但给出了每次训练步的时间分解：G2PO在优势计算阶段仅增加约1秒额外开销（CPU实现），总时间占比0.4%。
- 算力资源描述清晰，具备可复现性。

## 5. 实验数量与充分性
- **主实验**：在3个基准上，针对2个模型大小（1.5B、7B），对比9种基线，报告成功率和得分，结果平均3个随机种子，带标准差。
- **消融实验**：在Qwen2.5-1.5B上逐步添加组件（A_EP -> +A_NC -> +GA -> +A_EC），在WebShop和ALFWorld上验证各模块贡献。
- **超参数分析**：对平衡权重$w$进行调参（0.6~1.2），给出最佳值0.8。
- **方差分析**：展示了状态价值估计的方差分布，说明组聚合的必要性。
- **效率分析**：对比GRPO、GiGPO、G2PO推理步数，证明G2PO减少交互轮次。
- **计算成本分析**：分解每步时间，证明G2PO额外开销极小。
- **组大小分析**：展示了训练过程中平均组大小动态和分布，证明多数状态受益于聚合。
- **案例研究**：提供一个ALFWorld的具体图实例，说明边中心优势优于局部比较。
- **充分性评价**：实验覆盖主性能、消融、超参数、方差、效率、成本、案例分析，设计全面、公平（所有RL方法共享超参数和提示模板），结论可靠。

## 6. 主要结论与发现
- G2PO在所有三个基准上均一致优于所有对比方法，包括前沿闭源模型和现有RL方法。
- 相比GRPO，G2PO在WebShop上提升高达22.2%成功率，在ALFWorld上提升14.4%。
- 在AppWorld上，G2PO也取得最佳结果（成功率27.6%，得分21.7），验证其对复杂观测的泛化能力。
- 消融实验表明，组聚合状态价值估计和边中心优势估计是性能提升的关键。
- G2PO不仅提升准确性，还减少所需的交互步数，提升推理效率。
- 计算额外开销几乎可忽略（仅1秒/步），无需GPU即可完成优势计算。

## 7. 优点
- **创新性**：首次将状态转移图结构引入LLM智能体RL的信用分配，解决了线性轨迹视角的局限性。
- **技术巧妙**：通过组聚合降低方差，通过全局标准化TD误差捕捉全局重要性，设计合理。
- **高效**：优势计算全在CPU上完成，不增加GPU负担，实际开销极小。
- **实验严谨**：对比基线全面，结果带标准差，消融实验完整，算力资源公开。
- **实用性**：在多个具有挑战性的真实场景中显著超越SOTA，同时减少推理步数，兼具性能和效率。

## 8. 不足与局限
- **实验覆盖有限**：仅在三个基准上验证，且基础模型仅限于Qwen2.5系列（1.5B/7B/14B）。未在更大模型（如70B）或其他架构（Llama、Mistral）上测试，泛化性需进一步证明。
- **仅在verl-agent框架上实现**：作者提到仅在单一RL框架上实验，未在更多框架（如RAGEN、LOOP）上验证，可能限制了方法的适用性声明。
- **状态分组依赖观测相似性**：对于非结构化的观测（如自然语言描述），如何定义“相同观测”可能带来偏差；本文中假设环境返回相同文本即为相同状态，但在更复杂场景中可能不成立。
- **超参数w需调优**：平衡权重w对性能敏感（最佳0.8），需要在不同任务上调整，增加使用成本。
- **未探索树采样或异步采样**：作者在Limitations中指出，可扩展至树采样、异步采样等设置，未来工作方向。
- **理论证明有限**：虽然附录给出方差缩减的理论分析，但假设过于理想（如独立采样、状态组大小>1），实际训练中可能不完全满足。
- **奖励设计依赖规则**：使用了简单的规则奖励（成功/失败/无效惩罚），未涉及更复杂的奖励塑造方法。

（完）
