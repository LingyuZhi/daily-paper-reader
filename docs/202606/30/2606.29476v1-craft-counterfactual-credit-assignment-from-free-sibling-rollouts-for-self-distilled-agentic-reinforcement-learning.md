---
title: "CRAFT: Counterfactual Credit Assignment from Free Sibling Rollouts for Self-Distilled Agentic Reinforcement Learning"
title_zh: CRAFT：基于免费同辈 rollout 的反事实信用分配用于自蒸馏智能体强化学习
authors: "Zibin Meng, Kani Chen"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29476v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: CRAFT为自蒸馏的agentic RL引入反事实信用分配
tldr: 自蒸馏强化学习使用轨迹级奖励与token级蒸馏损失，但传统门控信号仅基于单个已实现轨迹且忽略符号方向。CRAFT提出三支柱信用分配方案：反事实token重要性利用GRPO的G-1兄弟rollout加权估计每个token的信用符号；不对称控制器根据门控活动指数移动平均动态调整蒸馏与KL权重；极化KL惩罚依据信用符号在模式寻找与覆盖间切换。实验在三个智能体环境、四个模型规模和五种方法上验证，CRAFT超越基线并具有位精确复现性。该工作解决了信号回顾性与符号盲问题，提供了理论一致性证明与方差界。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29476v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 859, \"height\": 280, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29476v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 370, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1539, \"height\": 455, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 872, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 865, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 883, \"height\": 1029, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29476v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 818, \"height\": 918, \"label\": \"Table\"}]"
motivation: 现有自蒸馏RL的信用分配信号仅回顾已实现轨迹且忽略符号，无法捕捉反事实情况下教师偏好动作的负面影响。
method: CRAFT包含三部分：反事实重要性加权利用兄弟rollout估计token级信用符号；不对称控制器动态调节蒸馏与KL权重；极化KL惩罚根据信用符号切换更新方向。
result: 在三个智能体环境、四个模型规模和五种端到端方法中，CRAFT优于所有基线，包括共享不对称控制器的Adaptive-CRINGE，且算法贡献与实现分离。
conclusion: CRAFT通过反事实信用分配显著提升自蒸馏RL性能，具有理论一致性、方差界及位精确可复现性，为智能体学习提供更精确的梯度信号。
---

## 摘要
自蒸馏智能体强化学习通过token级蒸馏损失增强轨迹级奖励，其教师模型是基于特权上下文条件化的同一策略。当前主流方法通过一个标量（师生对数概率差距）来门控该损失。该信号有双重局限性：它是回顾性的，仅评估实际实现的rollout，而不评估反事实的rollout；且是符号盲的，从未指示教师偏好动作何时会损害轨迹。我们提出CRAFT，一种三支柱信用分配方案，解决了上述两个局限性。支柱1，反事实Token重要性，重用GRPO已采样的G-1个同辈rollout，并通过对数概率差距进行重要性加权，形成对每步提升教师偏好动作所引起的优势变化的组级反事实变化的自归一化估计；这以接近零的额外计算代价产生有符号的逐token信用。支柱2是一个非对称控制器，当它沿着门控活动的指数移动平均降低参考KL权重时，提高蒸馏权重，反之亦然。支柱3逐token极化KL惩罚，根据信用的符号在模式搜索和模式覆盖更新之间切换。每个支柱都有一个独立开关，当禁用时，损失和梯度与基线在IEEE-754算术下字节一致，因此任何测量到的增益都可归因于算法变化而非实现漂移。我们证明了估计量的一致性和方差界限，提供了结构和位精确的可重复性保证，并在三个智能体环境、四个模型规模、五种端到端方法以及两个先前工作的表格化基线上评估了CRAFT。其中包括Adaptive-CRINGE，一个与CRAFT共享支柱2的比较器，以隔离反事实贡献的作用。

## Abstract
Self-distilled agentic reinforcement learning augments trajectory-level reward with a token-level distillation loss, using as its teacher the same policy conditioned on privileged context. The prevailing recipe gates this loss by a single scalar, the teacher-student log-probability gap. This signal is doubly limited: it is retrospective, scoring only the realised rollout and never the counterfactual ones, and it is sign-blind, never signalling when a teacher-preferred action would have harmed the trajectory. We introduce CRAFT, a three-pillar credit-assignment scheme that addresses both limitations. Pillar 1, Counterfactual Token Importance, reuses the G-1 sibling rollouts that GRPO already samples and importance-weights them by the log-probability gap to form a self-normalised estimate of the group-level counterfactual change in advantage from up-weighting teacher-preferred actions at each step; this yields a signed per-token credit at near-zero extra compute. Pillar 2 is an asymmetric controller that raises the distillation weight as it lowers the reference-KL weight along an exponential moving average of gate activity, and conversely. Pillar 3 polarises the KL penalty token by token, switching between a mode-seeking and a mode-covering update according to the sign of the credit. Each pillar has an independent switch that, when disabled, renders the loss and gradient byte-identical to the baseline in IEEE-754 arithmetic, so any measured gain is attributable to algorithmic change rather than implementation drift. We prove the estimator's consistency and a variance bound, give structural and bit-exact reproducibility guarantees, and evaluate CRAFT across three agentic environments, four model scales, and five end-to-end methods, plus two tabulated prior-work baselines. Among these is Adaptive-CRINGE, a comparator sharing Pillar 2 with CRAFT, isolating the counterfactual contribution.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **现有问题**：当前自蒸馏智能体强化学习（如 SDAR）使用一个标量门控信号——教师-学生对数概率差距 Δt——来加权 token 级蒸馏损失。该信号存在**双重局限**：
  - **回顾性（Retrospective）**：仅评估已实现的 rollout，无法考虑“如果采样教师动作会发生什么”的反事实场景。
  - **符号盲（Sign-blind）**：门控 σ(βΔt) 是 Δt 的单调函数，无法区分“教师动作在现实中会更好”还是“会更差”，导致即使教师动作会降低轨迹回报，模型仍被推向教师。
- **本文目标**：设计一种能利用 GRPO 组内已有并行 rollout（G-1 个同辈样本）来估计反事实信用，并生成有符号、前瞻性的 token 级信用信号的方法，同时保证算法的可解释性和可复现性。

### 方法论：核心思想、关键技术细节

- **总体框架**：CRAFT 在原有 GRPO 策略梯度损失和蒸馏损失基础上，添加三个可独立启用/禁用的支柱（Pillar），每个支柱通过开关控制，关闭时损失与梯度与基线字节一致（P7），确保增益来自算法改进。

- **支柱1：反事实 Token 重要性（CTI）**  
  - **核心思想**：利用 GRPO 组内 G-1 个同辈 rollout（j≠i），通过自归一化重要性采样估计“如果 token t 从教师采样，组级优势会如何变化”。每个同辈的权重为 exp(Δt(j)/τ)，形成自归一化重要性加权平均 \(\hat{CTI}_t^{(i)} = \sum_{j\neq i} w_t^{(j)} A^{(j)} - A^{(i)}\)。
  - **有符号信用 ct**：通过非对称 sigmoid 将 \(\hat{CTI}\) 压缩到 (-1,1)，正负方向独立调节（β+、β-），并 stop-gradient。  
  - **损失形式**：\(L_{CTI}(\theta) = -\mathbb{E}[ c_t \cdot \log \pi_\theta(y_t|s_t) ]\)，为 REINFORCE 风格。
  - **理论保证**：估计量一致（Prop 1），方差界 O(1/G)（Prop 2）。

- **支柱2：阶段感知自适应控制器**  
  - **动机**：蒸馏过程有活跃期、稳定期和饱和期，静态 λ、μ 不灵活。  
  - **控制律**：定义 ρgate(t) = 分数（|ct| > θg 的 token 比例）。维护 EMA：\(\rho_t^{\text{EMA}} = \alpha \rho_{t-1}^{\text{EMA}} + (1-\alpha) \rho_t^\text{gate}\)。  
    - λ(t) 随 ρEMA 增大而增大（蒸馏力度），μ(t) 随 ρEMA 增大而减小（KL 惩罚放宽）；反之亦然。  
  - **特性**：有固定点（P4），在前 nwarmup 步保持中间值，防止早期噪声。

- **支柱3：方向极化 KL**  
  - **动机**：无需对所有 token 应用相同 KL。ct>0 时应模式寻找（mode-seeking），ct<0 时应模式覆盖（mode-covering）。  
  - **规则**（θKL=0 默认）：
    - 若 ct > θKL：使用反向 KL 项 rev(t) = log πθ - log πref（向 πref 拉近）。  
    - 若 ct < -θKL：使用前向 KL 项 fwd(t) = -(log πθ - log πref)（推离 πref）。  
    - 否则：标准 KL 惩罚（k3）。  
  - **注意**：前向分支不是无偏 KL 估计，而是受 forward-KL 启发的更新规则。

- **可复现性**：所有开关默认关闭时，损失和梯度与基线在 IEEE-754 下字节一致（P7），通过 torch.equal 回归套件验证。

### 实验设计

- **环境与基准**：三个多轮智能体基准：
  - **ALFWorld**（文本虚拟家庭任务，成功率）  
  - **Search-QA**（来自 Search-R1，直接检索变体，精确匹配）  
  - **WebShop**（网购模拟，归一化奖励 0–100）
- **模型**：4 种开源 LLM：Qwen3-1.7B、Qwen2.5-3B、Qwen2.5-7B、Qwen3-8B。
- **对比方法**（5 种端到端 + 2 种表格基线）：
  - GRPO（无蒸馏）、RLSD（sign(A)·Δt 加权）、SDAR（原始单门）、Adaptive-CRINGE（CRINGE 负项 + CRAFT 支柱2）、CRAFT-Full（全三支柱）；另表格引用 GRPO+OPSD 和 Skill-SD。

### 资源与算力

- 小模型（1.7B/3B）在 **8×24 GB GPU** 上训练，大模型（7B/8B）在 **4×48 GB GPU** 上训练。
- 未明确给出训练总时长或批次大小，但提及“150 steps/cell”等细节。

### 实验数量与充分性

- **实验家族**（8 个大类）：
  - **E1**：主网格（表2），覆盖所有环境×模型×方法，CRAFT 在每个格子最优。  
  - **E2**：每支柱消融（表3），包括 +P1、+P1+P2、+P1+P3 及两个关键排除实验（A6：关掉 CTI 退化为 SDAR；A7：交换 KL 方向作为证伪测试）。  
  - **E3**：超参数消融（β、τ、θg 等）。  
  - **E4**：反事实消融（Gmin=999 测试 P3 降级）。  
  - **E5**：KL 方向交换（证伪 P5）。  
  - **E6**：快速预扫描。  
  - **E7、E8**：泛化与检索鲁棒性（表4：OOD 30% 留出、随机检索）。
  - **额外**：87 个回归测试（8 个字节精确、24 个数学正确、55 个工程契约）。
- **充分性评估**：实验设计系统、控制变量清晰（Adaptive-CRINGE 隔离支柱2），消融完整，有证伪测试，结论可信。

### 主要结论与发现

- **主要结果**：CRAFT-Full 在所有（环境×模型）格子上均为最佳。与 Adaptive-CRINGE 的差距证明了除支柱2外，P1+P3 提供额外增益（平均 +1~3% 绝对）。与 SDAR 相比平均提升 2–5%（小模型增益最大，符合缩放预测）。
- **消融证据**：支柱1（CTI）贡献主要增益，支柱2（控制器）贡献小型增量，支柱3（极化 KL）是自洽补充。关闭 CTI 后退化为 SDAR 基线（差距<0.2%），交换 KL 方向后性能低于基线（证伪成功）。
- **泛化性**：CRAFT 在 OOD 和随机检索场景下降最小，鲁棒性优于基线。

### 优点

1. **理论严谨**：给出估计量一致性（Prop1）和方差界（Prop2），控制器固定点（P4），以及位精确复现性（P7）。  
2. **可解释性与可复现性**：每个支柱有独立开关，关闭时完全恢复基线行为；87 个单元测试确保实现正确性。  
3. **高效性**：CTI 计算几乎免费（torch.no_grad() 下一次 softmax + 加权求和），无需额外模型评估。  
4. **信号质量改进**：同时利用组结构、教师偏好和轨迹回报，产生有符号前瞻性信用，比 CRINGE、RLSD、SDAR 都更丰富（表1）。  
5. **消融设计清晰**：Adaptive-CRINGE 比照组和 falsification test 使因果归因明确。

### 不足与局限

1. **组结构依赖**（L1）：需要组 rollout（G≥2），非组骨干（如 PPO single-path）无法直接使用，需未来探索合成同级。  
2. **超参数增多**（L2）：新增 9 个超参数（β±、τ、θg、[λmin,λmax]、[μmin,μmax]、α、nwarmup、θKL、Gmin），虽给出默认值，但调参负担仍在。  
3. **支柱3单独增益小**（L3）：极化 KL 是自洽补充而非性能提升主力，其贡献在完整性。  
4. **实验覆盖有限**（L4）：仅文本智能体环境，OOD 仅域内分割；缺少多模态、跨域泛化验证。  
5. **前向 KL 分支的不严格性**（L5）：负分支是启发式更新规则，不是无偏 KL 估计，可能带来未知偏差。  
6. **计算资源未详细披露**：未给出完整训练时间/能耗，影响复现效率评估。

（完）
