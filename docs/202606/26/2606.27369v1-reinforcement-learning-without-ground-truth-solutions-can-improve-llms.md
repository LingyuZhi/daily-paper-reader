---
title: Reinforcement Learning without Ground-Truth Solutions can Improve LLMs
title_zh: 无真实解强化学习可改进大语言模型
authors: "Yingyu Lin, Qiyue Gao, Nikki Lijing Kuang, Xunpeng Huang, Kun Zhou, Tongtong Liang, Zhewei Yao, Yi-An Ma, Yuxiong He"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27369v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 无真实答案的强化学习方法提升LLM能力
tldr: "针对缺乏真实解的强化学习训练场景，本文提出RiVER框架，利用确定性执行反馈作为连续奖励，通过校准奖励整形解决规模主导和频率主导问题。在12个AtCoder任务上训练，Qwen3-8B和GLM-Z1-9B-0414在ALE评分排名分别提升8.9%和9.4%。尽管未使用真实解，RiVER在精确解基准LiveCodeBench和USACO上分别提升2.4%和3.5%，表明基于评分优化的训练可有效提升通用编码能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1694, \"height\": 841, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 608, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1687, \"height\": 957, \"label\": \"Table\"}]"
motivation: 现有RLVR框架依赖真实解分配奖励，限制了对无真实解任务的适用性。
method: 提出RiVER框架，使用确定性执行反馈作为连续奖励，并通过实例间比较和优先顶级解进行校准。
result: "在ALE-Bench上评分排名提升8.9%~9.4%，在LiveCodeBench和USACO上绝对提升2.4%~3.5%。"
conclusion: 结合校准奖励的评分优化任务可替代真实解，有效提升LLM的通用编码能力。
---

## 摘要
基于可验证奖励的强化学习（RLVR）在训练大语言模型时通常依赖真实答案来分配奖励，这限制了其在真实解未知任务中的适用性。我们提出了一个**排名驱动的可验证框架**（RiVER），该框架在无真实解的评分优化任务上训练大语言模型，利用确定性执行反馈作为连续值监督。当对这类连续奖励应用组相对强化学习时，我们识别出两个关键挑战：**规模主导**（未校准的评分幅度在不同测试实例间扭曲策略更新）和**频率主导**（反复采样的次优解可能压倒罕见但更强的候选解）。RiVER通过校准奖励塑造来解决这些挑战：使用实例级比较，并强调排名靠前的求解器，同时为其他有效解保留有界反馈。我们在12个AtCoder启发式竞赛任务上训练，并在算法工程基准（ALE-Bench）、LiveCodeBench和USACO上评估。RiVER将Qwen3-8B和GLM-Z1-9B-0414的ALE评分排名分别提升了8.9%和9.4%。更重要的是，尽管仅在无任何真实解的评分任务上训练，RiVER在LiveCodeBench和USACO等精确解基准上也使骨干模型平均绝对提升了2.4%和3.5%。相比之下，使用原始执行分数训练的基线模型提高了ALE评分，但未能迁移到精确解基准。这些结果表明，评分优化任务结合适当的奖励校准，可以在无真实解的情况下作为通用编码能力的有效训练环境。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) for training LLMs typically rely on ground-truth answers to assign rewards, limiting their applicability to tasks where the ground-truth solution is unknown. We introduce a \textbf{R}anking-\textbf{i}nduced \textbf{VER}ifiable framework (RiVER) that trains LLMs on score-based optimization tasks without ground-truth solutions, using deterministic execution feedback as continuous-valued supervision. When applying group-relative RL to such continuous rewards, we identify two key challenges: \emph{scale dominance}, where uncalibrated score magnitudes across test instances distort policy updates, and \emph{frequency dominance}, where repeatedly sampled suboptimal solutions can outweigh rare but stronger candidates. RiVER addresses these challenges with calibrated reward shaping that uses instance-wise comparisons and emphasizes top-ranked solvers while retaining bounded feedback for other valid solutions. We train on 12 AtCoder Heuristic Contest tasks and evaluate on Algorithm Engineering Benchmark (ALE-Bench), LiveCodeBench, and USACO. RiVER advances Qwen3-8B and GLM-Z1-9B-0414 by 8.9\% and 9.4\% in ALE rating rank. More importantly, despite training exclusively on score-based tasks without any ground-truth solutions, RiVER also improves the backbones across exact-solution benchmarks such as LiveCodeBench and USACO by an absolute average improvement of 2.4\% and 3.5\%. By contrast, baselines trained with raw execution scores improve ALE rating but fail to transfer to exact-solution benchmarks. These results suggest that score-based optimization tasks, combined with proper reward calibration, can serve as effective training environments for general coding ability without ground-truth solutions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的基于可验证奖励的强化学习（RLVR）在训练大语言模型时，通常依赖**真实答案**（如标准答案、单元测试）来分配二值奖励。然而，许多实际任务（如算法设计、优化、规划）并没有单一已知的最优解，可行解的质量可以通过目标函数比较，但无法获得“真实解”。这限制了RLVR的应用范围。
- **研究动机**：能否利用**无真实解但可执行校验**的优化任务（如AtCoder启发式竞赛）作为训练环境，通过比较候选解的相对优劣提供密集学习信号，从而提升通用推理和编码能力？
- **整体含义**：本文证明，通过恰当的奖励校准（如排名和优先获胜者），基于评分的优化任务可以替代真实解，提供有效的训练信号，并且学到的能力可以**迁移到精确解基准**（如LiveCodeBench、USACO），扩展了RLVR的范式。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将无真实解的优化任务转化为组相对强化学习信号，通过**实例级排名**和**优先获胜者的奖励整形**克服原始连续奖励的两个关键问题：规模主导（不同实例评分尺度不同导致更新偏差）和频率主导（反复采样的次优解累积梯度压倒罕见优解）。
- **关键技术细节**：
    1. **执行环境**：每个问题提供问题描述 \(q\)、确定性评估器 \(E\) 和一组隐藏测试实例 \(\{T_m\}\)。模型生成包含代码的响应，仅执行代码，获得有效性指示 \(v(c, T_m)\) 和连续目标分数 \(f(c, T_m)\)。
    2. **实例级排名**：对每个隐藏实例 \(T_m\)，将 \(G\) 个候选求解器按分数排名（处理平局时用平均秩），消除实例间尺度差异。
    3. **优先获胜者整形**：定义 \(s_{i,m}\)：
        - 无效输出：\(-1\)；
        - 最佳有效求解器（秩=1）：\(1\)；
        - 其他有效非获胜者：\(\text{clip}((G-1 - r_{i,m})/(G-3), 0, 1) - 0.5\)，提供 \([-0.5, 0.5]\) 内有界反馈。
    4. **聚合与GRPO更新**：跨实例平均得到 \(\bar{s}_i\)，作为样本级优势 \(\hat{A}_i\)，直接用于GRPO的裁剪目标和KL正则化，无需额外标准化。
- **核心公式**（文字说明）：基于GRPO框架，将标准二值或原始分数奖励替换为上述排名诱导的优先获胜者优势。

### 3. 实验设计：数据集、基准、对比方法

- **训练数据集**：AtCoder Heuristic Contest (AHC) 的最新12个任务（AHC047–AHC062中选取，排除与ALE-Bench测试集重叠的及不兼容的一轮设置的任务）。每个任务包含问题描述、官方评估器和测试实例生成器。
- **评估基准**：
    - **评分任务基准**：ALE-Bench（基于AHC问题），报告Rating和Rank%（排名百分比，越低越好）。
    - **精确解基准**：LiveCodeBench v5/v6（按Easy/Med/Hard报告Pass@1）、USACO（Pass@1）。
- **对比方法**（在Qwen3-8B和GLM-Z1-9B-0414两个骨干模型上比较）：
    - Raw-GRPO：直接使用原始执行分数聚合。
    - RS-GRPO：风险敏感变换。
    - Raw-Binary：赢家通吃（最高分得1，其余0）。
    - Instance-Norm：每个实例内标准化分数后平均。
    - Rank-uniform：实例内排名但采用均匀奖励分配。
    - **RiVER**（本文方法）。

### 4. 资源与算力

- 文中提到训练细节：使用 **AdamW优化器**，学习率 \(1\times10^{-6}\)，KL系数0.001，组大小 \(G=16\)，全局batch size 64。**未明确说明**使用的GPU型号、数量及总训练时长。因此无法给出具体算力信息。

### 5. 实验数量与充分性

- **实验数量**：主要实验包括表1（两个骨干模型 × 6种方法 × 4个基准，共约48组结果）；训练动态图（Figure 2）展示12个AHC任务上最佳分数曲线；案例研究（AHC057）对比三种求解器代码。
- **消融实验**：对比了多种奖励设计（Raw-GRPO、RS-GRPO、Raw-Binary、Instance-Norm、Rank-uniform），有效验证了实例级排名和优先获胜者整形的贡献。
- **充分性**：实验覆盖了评分和精确解两类基准，包含不同难度水平，且训练数据与评估无重叠（避免污染）。消融设计清晰，能够说明各组件作用。但**不足**：训练仅12个AHC任务，可能限制泛化结论；未在更多类型优化任务（如Topcoder）上验证；未报告统计显著性检验或多种子数结果（仅提到三次独立运行取平均）。

### 6. 论文的主要结论与发现

1. **RiVER显著提升评分任务表现**：在ALE-Bench上，Qwen3-8B评分排名提升8.9%（142分），GLM-Z1-9B提升9.4%（157分）。
2. **RiVER能够迁移到精确解基准**：尽管仅在无真实解的评分任务上训练，RiVER在LiveCodeBench和USACO上平均绝对提升2.4%和3.5%，而所有使用原始分数的基线均未展示一致迁移。
3. **奖励校准至关重要**：原始连续奖励（Raw-GRPO、RS-GRPO、Raw-Binary）虽提升评分任务，但无法迁移；仅有排名+优先获胜者整形的方法实现了跨基准改善。
4. **训练难度分析**：提升主要集中在LiveCodeBench的中等和困难问题上，易题（基础模型已>93%准确率）几乎无变化，说明训练增强了处理复杂推理的能力。

### 7. 优点：方法或实验设计上的亮点

- **问题视角新颖**：将无真实解的可执行优化任务作为RLVR训练环境，扩展了RLVR的适用范围。
- **挑战识别清晰**：明确提出“规模主导”和“频率主导”两个弊端，并设计针对性解决方案。
- **方法简洁有效**：RiVER仅通过实例级排名和优先获胜者整形两个简单操作，无需额外模型或复杂工程，即可实现稳定训练和跨基准迁移。
- **实验设计严谨**：训练集与评估集严格分离，排除重叠；对比多种奖励设计，消融充分；结果统计可靠（三次独立运行平均）。
- **信息论视角**：从Rényi熵角度形式化解释了密集反馈相对于稀疏二值反馈的优势，提供了理论直觉。

### 8. 不足与局限

- **训练任务多样性有限**：仅使用12个AtCoder启发式竞赛任务，类别较为单一（均为算法优化问题），通用性尚需在更多类型优化任务（如Topcoder、真实世界规划）上验证。
- **算力信息不透明**：未报告GPU型号、数量及训练时长，不利于复现和评估资源需求。
- **统计严谨性不足**：仅报告平均Pass@1，未提供方差或置信区间；未进行假设检验。
- **迁移现象的解释尚浅**：论文观察到评分任务训练能改善精确解基准，但未深入分析具体学习到的能力是什么（例如是否学会了更好的代码结构、调试策略或算法设计），缺乏机制分析。
- **应用限制**：依赖可执行环境和确定性评估器，不适用于无法执行或主观评判的任务（如开放式写作、对话生成）。此外，训练环境需能快速产生有效反馈，否则采样成本高。

（完）
