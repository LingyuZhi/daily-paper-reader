---
title: Joint Learning of Experiential Rules and Policies for Large Language Model Agents
title_zh: 大型语言模型智能体的经验规则与策略联合学习
authors: "Shicheng Ye, Chao Yu"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27136v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 针对大语言模型智能体的强化学习方法
tldr: 针对LLM智能体在多步交互环境中有效利用积累经验的问题，现有方法将经验分离为外部自然语言规则或用于更新模型参数，但存在规则与策略不同步或局部纠正有限的问题。本文提出JERP方法，从相同交互轨迹联合更新长期经验规则池和策略，决策时检索规则与历史共同推理，交互后同时优化策略和修正规则。在AlfWorld和WebShop任务上，JERP持续提升决策性能，实现了规则与策略的协同进化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27136v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1821, \"height\": 681, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 864, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 727, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1866, \"height\": 917, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27136v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 936, \"height\": 358, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27136v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 385, \"label\": \"Table\"}]"
motivation: 现有方法将交互经验分离为外部规则或参数更新，前者易与策略脱节，后者在稀疏奖励下局部纠正有限。
method: JERP从相同轨迹联合更新规则池和策略，决策时检索规则与历史共同推理，交互后同时优化策略并修正规则。
result: 在AlfWorld和WebShop任务上，JERP相比基线方法取得一致的决策性能提升。
conclusion: JERP通过规则与策略的联合学习，实现经验的有效利用，为LLM智能体在复杂交互任务中提供稳定改进。
---

## 摘要
对于多步交互环境中的大语言模型智能体，一个关键挑战是如何有效利用积累的交互经验。现有工作通常将这类经验分为两种用途：以自然语言规则的形式保留在模型外部供后续提示使用，或利用轨迹和反馈来更新模型参数。前者易于解释，但可能随着策略演变而不同步；后者能更广泛地改进策略，但在稀疏奖励设置下仅能对局部错误提供有限修正。我们提出针对LLM智能体的经验规则与策略联合学习（JERP），该方法从同一交互轨迹更新长期经验规则池和策略。在决策时，JERP检索任务相关规则，并将其与交互历史一起作为智能体的条件。每轮后，它利用收集的轨迹优化策略，并通过对比当前展开与参考成功轨迹来修正规则池。这种耦合使规则池与不断演变的策略保持一致，同时允许稳定有效的行为逐渐被模型自身吸收。在AlfWorld和WebShop上的实验表明，JERP在复杂交互任务的决策性能上取得了一致提升。

## Abstract
For LLM agents in multi-step interactive environments, a key challenge is to make effective use of accumulated interaction experience. Existing work has typically separated two uses of such experience: keeping it outside the model as natural-language rules for later prompting, or using trajectories and feedback to update the model parameters. The former is easy to interpret but can fall out of sync with the evolving policy; the latter improves the policy more broadly but provides only limited correction for local mistakes in sparse-reward settings. We present Joint Learning of Experiential Rules and Policies for LLM Agents (JERP), which updates a long-term experiential-rule pool and the policy from the same interaction trajectories. At decision time, JERP retrieves task-relevant rules and conditions the agent on them together with the interaction history. After each episode, it uses the collected trajectories both to optimize the policy and to revise the rule pool by comparing current rollouts with reference successful trajectories. This coupling keeps the rule pool aligned with the evolving policy while allowing stable and effective behaviors to be gradually absorbed into the model itself. Experiments on AlfWorld and WebShop show that JERP yields consistent gains in decision performance for complex interactive tasks.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的中文总结。

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）

大语言模型智能体在多步交互环境（如网页导航、具身交互）中面临如何有效利用积累的交互经验的关键挑战。现有方法通常将经验用于两种分离的用途：
- **外部规则**：将经验转化为自然语言规则（如反思文本、操作手册），在后续提示中提供上下文。优点是可解释、可编辑；缺点是规则可能随策略演变而过时，两者脱节。
- **参数更新**：利用轨迹和反馈直接更新模型参数（如RL）。优点是可全局改善策略；缺点是在稀疏奖励设置下，局部错误可能得不到及时精准的纠正。

本文认为，这两种用途具有互补的局限性，并研究能否从同一交互数据同时支持规则更新和策略优化，提出**JERP（经验规则与策略联合学习）** 方法。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：在训练循环中同时更新策略参数和一个长期经验规则池。决策时检索任务相关规则作为上下文；每轮结束后，使用同一批轨迹既进行基于组的策略优化，又通过对比反思更新规则池。

**关键技术细节**：
- **规则池设计**：为每个任务实例维护一个规则池 \(K(d)\)，包含规则文本 \(z_i\) 和效用分数 \(s_i\)。决策时根据分数降序选取 top-k 规则作为工作集 \(\tilde{K}(d)\)。
- **轨迹采样**：对于任务 \(d\)，策略 \(\pi_\theta\) 在给定当前历史 \(h_t\) 和工作规则集 \(\tilde{K}(d)\) 的情况下采样动作，生成 N 条完整轨迹组成轨迹组 \(T_d\)。
- **模型参数更新（阶段 I）**：采用组相对策略优化（GRPO）目标函数：
  - 计算轨迹组内每条轨迹的标准化奖励作为优势 \(A_i\)。
  - 使用截断的重要性采样比和 KL 正则化项计算梯度，更新策略参数 \(\theta\)。
  - 公式：\(L_{\text{GRPO}}(\theta) = -\frac{1}{N}\sum_{i=1}^N \frac{1}{|\tau_i|}\sum_{t=0}^{|\tau_i|-1} \left[ \min(\rho_{i,t}(\theta)A_{i,t}, \bar{\rho}_{i,t}(\theta)A_{i,t}) - \beta D_{\text{KL}}(\pi_\theta \| \pi_{\text{ref}}) \right]\)。
- **经验规则更新（阶段 II）**：通过对比反思更新规则池：
  - 输入：当前规则池 \(K(d)\)、当前轨迹组 \(T_d\)、参考成功轨迹集 \(T^+(d)\)（累积的成功轨迹）。
  - 利用冻结参数的LLM生成结构化编辑操作：ADD（新增规则）、EDIT（修改规则）、UPVOTE/DOWNVOTE（增减效用分数）、MERGE（合并相似规则）。
  - 应用操作后更新分数，低于阈值的规则被移除，保持容量限制。
- **整体算法**：见 Algorithm 1，每次迭代采样一个任务实例，采样 N 条轨迹，计算优势，更新参数，更新规则池，并更新参考成功轨迹集。

### 3. 实验设计

**数据集/场景**：
- **AlfWorld**：文本家庭操作环境，包含6种子任务：Pick、Clean、Heat、Cool、Look、Pick2。部分可观察，需多步交互。
- **WebShop**：在线购物环境，根据用户需求执行搜索、浏览、比较、购买等操作。

**评估指标**：
- AlfWorld：任务成功率（%）。
- WebShop：平均得分和任务成功率（%）。

**对比方法**：
- Vanilla LLM：直接提示，无推理框架或记忆。
- ReAct：结合推理与行动，仅使用当前回合上下文。
- Reflexion：利用自然语言反思作为跨回合记忆。
- RLOO：使用REINFORCE留一法基线更新策略。
- GRPO：基于组相对比较更新策略（无独立价值模型）。
- 所有基于RL的方法（RLOO、GRPO、JERP）均使用LoRA进行参数高效微调。

**关键设置**：每组实验取三次独立运行的平均值；超参数见表 I（如学习率3e-6，组大小8，训练epochs: AlfWorld 200, WebShop 300）。

### 4. 资源与算力

论文在实验部分明确说明：
- **硬件**：Ubuntu 22.04 LTS服务器，配备两个Intel Xeon Gold 6226R处理器、约512GB内存、四块NVIDIA A30 GPU。
- **软件**：无特定框架提及，但使用了LoRA微调。
- **训练时间**：未给出具体训练时长，仅提及训练epochs（AlfWorld 200，WebShop 300）。因此无法直接推断总计算成本。

### 5. 实验数量与充分性

- **主要实验**：在两个环境上对比5个基线方法（表II），并展示了AlfWorld各子任务的训练曲线（Fig.4）。
- **消融实验**：构建了一个“固定规则池”变体（仅初始化时更新一次规则，后续冻结），与完整JERP对比在AlfWorld上的成功率曲线（Fig.3）。结果显示持续更新规则池对性能提升至关重要。
- **充分性评估**：实验覆盖了两种不同的交互环境，包含了从无记忆到有记忆、从无训练到有训练的多种基线，且对关键组件（规则池动态更新）进行了消融，实验设计比较全面和公平。但未提供统计显著性检验（如置信区间），仅在文本中称“三次独立运行平均”。

### 6. 论文的主要结论与发现

- JERP在AlfWorld和WebShop上均取得最佳总体性能：AlfWorld成功率61.5%（优于GRPO的57.8%），WebShop成功率64.1%（优于RLOO的57.8%和GRPO的56.2%）。
- 增益主要集中在约束更复杂、序列更长的子任务（如Clean、Heat、Cool、Pick2），而在简单任务（Pick、Look）上GRPO略优，说明规则池对解决中间约束特别有效。
- 消融实验表明，持续更新规则池（而非一次性固定）是获得稳定性能提升的关键。
- 结论：将外部规则学习与参数优化耦合在同一训练循环中，可以使局部可重用经验以规则形式保存，同时将稳定行为逐渐吸收到策略中。

### 7. 优点

- **方法创新性**：首次将规则池的在线维护与策略的强化学习更新深度融合，解决了规则过时与策略演进之间的矛盾。
- **实用可解释性**：规则以自然语言形式存在，可检查、可编辑，增强了透明性。
- **泛化性**：在两种不同领域（文本操作、网页购物）上均验证了有效性，且对长序列任务尤其明显。
- **消融验证充分**：通过固定规则池的对比，清晰证明了规则动态更新的必要性。
- **实验公平性**：所有参与训练的方法均使用相同的LoRA设置和基本超参数，对比基线涵盖了主流方法。

### 8. 不足与局限

- **规则检索过于简单**：当前仅按效用分数在任务级别排序，未考虑每个任务实例内的细粒度相关性，可能限制了规则利用效率。论文也承认可以更精细的实例级检索。
- **规则更新依赖LLM的反思能力**：使用冻结LLM生成编辑操作，其质量和一致性可能受限于模型本身，且引入额外计算开销。
- **实验覆盖范围有限**：仅测试了两个文本交互环境（AlfWorld和WebShop），未涉及具身机器人、代码生成等更广泛场景；也未与最新的RL方法（如DPO变体、在线RLHF）比较。
- **未提供计算成本细节**：未报告训练时长、总token消耗，难以评估方法的实际部署成本。
- **统计显著性未明确**：虽报告多次运行平均，但未给出标准差或置信区间，读者难以评估结果的可重复性。
- **适用性限制**：在高实时性低延迟场景下，决策时检索规则可能增加延迟；另外，规则池随训练扩大可能带来维护压力。

（完）
