---
title: "From Verdict to Process: Agentic Reinforcement Learning for Multi-Stage Fact Verification"
title_zh: 从结果到过程：面向多阶段事实验证的智能体强化学习
authors: "Rongxin Yang, Shenghong He, Siyuan Zhu, Chao Yu"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13262v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 提出用于事实验证的智能体强化学习框架
tldr: 多阶段事实验证通常由分解、检索、生成和预测等模块串联执行，但现有方法孤立优化各阶段或依赖固定启发式，导致协调不足。本文提出ProFact，使用智能体强化学习对验证轨迹进行端到端优化，并设计过程感知奖励提供阶段级学习信号，缓解最终标签的稀疏延迟监督。实验表明，ProFact在验证准确率和推理效率上均显著优于强基线，验证了轨迹级优化在多阶段推理中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13262v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1260, \"height\": 781, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13262v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1015, \"height\": 738, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13262v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1180, \"height\": 1023, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13262v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 932, \"height\": 493, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13262v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1102, \"height\": 529, \"label\": \"Table\"}]"
motivation: 现有事实验证方法优化各阶段孤立或依赖启发式，缺乏自适应协调，导致联合决策次优。
method: ProFact通过统一策略协调四个子任务（分解、检索、生成、预测），并引入过程感知奖励提供中间监督。
result: 在多个基准上，ProFact在验证精度和推理效率上均超越最强基线，验证了轨迹级端到端优化的优势。
conclusion: 过程感知的强化学习可有效优化多阶段事实验证的协调策略，为复杂推理流水线提供新范式。
---

## 摘要
近期将大语言模型（LLMs）与检索增强推理相结合的方法在自动化事实验证方面展现出潜力。为处理复杂声明，这些验证流程通常执行多阶段工作流，协调紧密耦合的模块，包括声明分解、证据收集和结果预测。然而，现有方法孤立地优化各个阶段或依赖固定启发式规则，限制了阶段间的自适应协调，可能导致次优结果。本文提出ProFact，一种面向多阶段事实验证轨迹端到端优化的智能体强化学习框架。ProFact训练统一策略来协调声明分解、证据查找、答案生成和结果预测。为解决最终真实性标签提供的稀疏且延迟的监督信号，ProFact引入过程感知奖励，在验证全过程提供阶段级学习信号。实证评估表明，ProFact在验证性能和推理效率上均持续优于强基线。这些结果凸显了过程感知的轨迹优化对多阶段事实验证的有效性。

## Abstract
Recent approaches combining Large Language Models (LLMs) with retrieval-augmented reasoning have shown promise for automated fact verification. To process complex claims, these verification pipelines typically execute multi-stage workflows that coordinate tightly coupled modules, including claim decomposition, evidence gathering, and verdict prediction. However, existing methods optimize individual stages in isolation or rely on fixed heuristics, which limits adaptive coordination among stages and can lead to suboptimal outcomes. In this work, we propose ProFact, an agentic reinforcement learning framework for end-to-end optimization of multi-stage fact verification trajectories. ProFact trains a unified policy to coordinate claim decomposition, evidence seeking, answer generation, and verdict prediction. To address the sparse and delayed supervision provided by final veracity labels, ProFact introduces process-aware rewards that provide stage-level learning signals throughout the verification process. Empirical evaluation shows that ProFact consistently outperforms strong baselines in both verification performance and inference efficiency. These results highlight the effectiveness of process-aware trajectory optimization for multi-stage fact verification.

---

## 论文详细总结（自动生成）

# 从结果到过程：面向多阶段事实验证的智能体强化学习——论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：开放域事实验证是一个多阶段决策过程，包括声明分解、证据检索、证据驱动的答案生成和结果预测。现有方法孤立优化各阶段或依赖固定启发式规则，导致阶段间缺乏自适应协调，最终验证效果次优。
- **背景**：LLM结合检索增强推理在事实验证中显示出潜力，典型工作如InFact、HerO、DebateCV采用结构化流水线，但各阶段模块独立训练或提示，无法针对最终验证目标进行联合优化。最终真值标签的监督信号稀疏且延迟，难以对中间决策进行有效信用分配。
- **整体含义**：论文主张将事实验证建模为长时域决策问题，通过智能体强化学习实现端到端轨迹优化，从而自动协调分解、检索、回答和预测，提升验证准确性和效率。

## 2. 论文提出的方法论

### 核心思想

- **ProFact**：一个基于智能体强化学习的框架，将多阶段事实验证形式化为有限马尔可夫决策过程（MDP），使用统一策略πθ依次执行三个阶段：Question（生成验证问题）→ Search（检索证据并生成答案）→ Verdict（预测真值标签）。通过GRPO（Group Relative Policy Optimization）对完整验证轨迹进行端到端优化。
- **关键挑战**：最终标签监督稀疏延迟，为此设计**过程感知奖励函数**，在问题和搜索阶段提供基于METEOR匹配分数的中间奖励，在判决阶段提供二元正确性奖励。

### 关键技术细节

1. **MDP定义**：
   - 状态空间S：包含当前声明、已生成问题、QA对、推理阶段、已检索证据。
   - 动作空间A：每个阶段生成对应动作（问题、工具调用、答案、判决）。
   - 奖励r：过程感知（见公式6）。
   - 最大决策时域T=12步（含三个阶段，搜索阶段可多次迭代）。

2. **搜索阶段设计**：
   - 使用Qwen3-Embedding-0.6B构建kNN索引，检索预构建的证据库（AVeriTeC提供的静态知识存储）。
   - 每次搜索返回top-3证据项，证据驱动的答案由LLM生成。
   - 每个验证问题独立检索并清空上下文（上下文隔离）。

3. **过程感知奖励函数**（公式6）：
   - Question阶段奖励：预测问题集合与标注问题集合的METEOR最大二分匹配得分。
   - Search阶段奖励：预测的QA对与标注QA对的METEOR匹配得分（将问题和答案拼接成字符串后匹配）。
   - Verdict阶段奖励：预测标签与真实标签是否一致（0/1奖励）。
   - 无效/空输出奖励为0。

4. **优化算法**：
   - 采用GRPO（DeepSeekMath提出），每组采样G=8条轨迹，计算组内轨迹回报的均值和标准差，得到组相对优势（公式2）。
   - GRPO目标（公式3）：包含裁剪的策略比率、KL正则化项（系数β=0.001）。

5. **训练流程**：
   - 从旧策略πθ_old采样一组轨迹，计算过程感知回报，归一化为优势，再使用GRPO损失更新策略。

### 算法流程（文字说明）

- 输入：声明-标签对(c,y)，策略πθ，证据库K，时域T。
- 初始化状态s1为声明+Question阶段。
- 循环t=1..T：
  - 根据当前状态，策略生成动作at。
  - 根据阶段判断：Question阶段生成问题集，计算rq；进入Search阶段（初始化第一个问题）。
  - Search阶段：若at是工具调用 → 检索证据，更新状态；若at是答案 → 记录当前QA对，若全部回答则计算rs并进入Verdict阶段，否则换下一个问题。
  - Verdict阶段：解析预测标签，计算rv，结束。
- 返回轨迹τ和总回报R(τ)。

## 3. 实验设计

### 数据集与Benchmark

- **AVeriTeC**（Schlichtkrull et al., 2023）：面向真实世界事实验证的开放域数据集，包含声明、真实性标签（Supported/Refuted/Not Enough Evidence/Conflicting Evidence）以及标注的问答证据链。提供预收集的静态知识库。
- 评价指标：
  - **Q-only METEOR**：生成问题与标注问题的匹配度。
  - **Q&A METEOR**：生成的QA对与标注QA对的匹配度。
  - **Accuracy**：最终标签分类准确率。
  - **AVeriTeC Score**：官方综合评价指标，要求证据得分超阈值λ=0.25且标签正确。
- 使用开发集（dev set）报告结果。

### 对比方法

- **Consistency**：基于提示词基线，使用固定的标注QA作为证据输入，通过多次生成并一致性聚合预测标签。
- **InFact**（Rothermel et al., 2024）：六阶段静态流水线，包含声明重写、问题生成、检索、回答、推理、判决，端到端使用同一LLM。
- **HerO**（Yoon et al., 2024）：使用假设事实文档增强检索，采用独立组件进行问题生成和判决预测（问题生成和检索部分基于开源模型，判决使用闭源或开源LLM）。

**公平性保证**：所有方法使用相同证据源（AVeriTeC静态知识库），相同的检索预算（每次搜索返回top-3，最多5个问题）。

### 骨干模型

- 4个开源模型：Qwen2.5-3B-Instruct、Qwen2.5-7B-Instruct、Qwen3-4B-Instruct-2507、Qwen3-8B（使用默认思考模式）。分别记为Qwen2.5-3B/7B、Qwen3-4B/8B。

## 4. 资源与算力

- **论文未明确提及**GPU型号、数量及训练时长。
- 训练细节中提到：每组采样8条轨迹，mini-batch size=32，micro-batch size=4，KL系数0.001。但未给出具体硬件配置和耗时。因此资源算力信息不充分，无法评估训练成本。

## 5. 实验数量与充分性

- **主要实验**：主表（表1）包含4个骨干模型、4种方法（Consistency、InFact、HerO、ProFact），以及一个消融变体（w/o PR），共4×5=20个条件。同时报告4个指标（Q-only、Q&A、Accuracy、AVeriTeC Score），基本覆盖中间质量和最终性能。
- **消融实验**：去除过程奖励（w/o PR）的对比，验证奖励设计必要性。
- **效率分析**（表2）：对比ProFact与InFact在时间、输入/输出token上的差异，覆盖4个骨干。
- **RL算法对比**（表3）：在Qwen2.5-3B和7B上比较PPO、GRPO、DAPO、GiGPO，共4×2=8个条件。
- **充分性评估**：实验设计较为全面，涵盖了不同骨干规模、不同基线方法、关键消融和算法选择。但缺少在更多数据集（如FEVER、MultiFC）上的泛化实验，也缺少对检索策略（如不同top-k）的敏感性分析。总体而言实验在单一数据集上较为充分，但泛化性验证有限。

## 6. 主要结论与发现

1. **ProFact在验证性能上全面超越强基线**：在所有骨干模型上，AVeriTeC Score和Accuracy均为最高，且在大多数情况下Q-only METEOR也最优。
2. **过程感知奖励至关重要**：移除中间过程奖励后（w/o PR），性能显著下降（AVeriTeC Score平均下降约10-12个百分点），证明最终标签稀疏监督不足以有效训练。
3. **推理效率显著提升**：与InFact相比，ProFact在时间和token消耗上大幅降低（例如Qwen2.5-3B：7.29秒 vs 16.32秒，总token 7.64M vs 55.69M）。
4. **GRPO是适合的优化算法**：与PPO、DAPO、GiGPO相比，GRPO在验证指标上表现最佳，原因在于其组相对优势无需额外价值函数，且能稳定训练。
5. **骨干模型规模与性能非单调**：较大模型（如Qwen3-8B）并不一定获得更高准确率，作者归因于逆缩放现象（Inverse Scaling），即大模型可能更依赖参数化知识而非证据。

## 7. 优点

- **方法创新**：首次将多阶段事实验证整体建模为MDP，通过强化学习端到端优化，统一策略协调所有阶段，突破独立模块优化局限。
- **奖励设计合理**：过程感知奖励利用METEOR匹配提供密集信号，有效解决信用分配问题，且基于标注数据自动构建（无需人工标注每步好坏）。
- **效率兼顾**：通过上下文隔离和训练引导，减少冗余推理和检索，在提升性能的同时降低计算开销。
- **实验公平性**：所有方法共用同一证据源和检索预算，骨干模型一致，避免因外部模块差异导致的不公平。

## 8. 不足与局限

- **数据集单一**：仅使用AVeriTeC一个数据集进行实验，未在FEVER、MultiFC等其他常见事实验证基准上评估，泛化能力存疑。
- **对标注数据依赖**：过程感知奖励需要标注的验证问题和QA对，而许多数据集不提供此类中间标注，限制了方法的适用范围。
- **检索策略固定**：采用简单的kNN向量检索，未探索其他检索机制（如混合检索、重排序），也未与最新工具学习方法（如Search-R1）进行对比。
- **缺少大模型上训练的可扩展性分析**：最大模型仅8B，且训练细节中未说明大模型上的收敛行为和资源消耗，对于更大模型（如32B/70B）的效果未知。
- **未与闭源最强系统对比**：基线中的InFact、HerO使用开源或小模型，而当前最优事实验证系统可能依赖闭源LLM（如GPT-4），论文未与其直接比较，结论在同一开源生态内有效。
- **设备与算力信息缺失**：无法复现或评估训练成本，客观性略有不足。

（完）
