---
title: "GraphPO: Graph-based Policy Optimization for Reasoning Models"
title_zh: GraphPO：用于推理模型的图基策略优化
authors: "Yuliang Zhan, Xinyu Tang, Jian Li, Dandan Zheng, Weilong Chai, Jingdong Chen, Jun Zhou, Ge Wu, Wenyue Tang, Hao Sun"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18954v1"
tags: ["query:agentic-rl"]
score: 8.0
evidence: 基于图的策略优化用于增强推理模型的可验证奖励强化学习
tldr: 现有基于验证奖励的强化学习（RLVR）独立采样响应导致冗余探索且稀疏奖励难以识别有效步骤。本文提出GraphPO，将推理过程表示为有向无环图，合并语义等价路径，并赋予边效率与正确性优势，从而降低优势估计方差并提升推理效率。实验表明GraphPO在多种大语言模型上优于链式与树式基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18954v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 731, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1446, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1431, \"height\": 867, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1452, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1442, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 513, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 513, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18954v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 519, \"height\": 366, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18954v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 1051, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18954v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18954v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 357, \"label\": \"Table\"}]"
motivation: RLVR独立采样响应导致冗余探索，且稀疏奖励难以提供细粒度监督。
method: 将推理过程构建为有向无环图，节点为语义状态，边为推理步；合并等价路径并引入效率与正确性优势函数。
result: 在三个大语言模型上，GraphPO以相同token或响应预算一致优于链式和树式基线。
conclusion: GraphPO通过图结构共享信息，降低方差并提升探索效率，为推理模型强化学习提供新范式。
---

## 摘要
可验证奖励的强化学习（RLVR）已成为增强大型推理模型能力的标准范式。RLVR通常独立采样响应，并基于最终答案优化策略。该范式存在两个局限性：首先，独立响应常包含相似的中间推理步骤，导致冗余探索和计算浪费；其次，稀疏的最终答案奖励使得难以识别有用步骤。基于树的方法通过共享前缀并比较同一前缀下的分支以提供细粒度信号，部分解决了该问题。然而，树分支仍是独立扩展的。当不同分支达到相似推理状态时，它们无法共享信息并重复类似的探索。此外，基于树的方法忽略此类分散性，仅在各分支内进行局部比较，可能导致优势估计的方差更高。为应对这一挑战，我们提出GraphPO（图基策略优化），一种新颖的强化学习框架，将展开轨迹表示为有向无环图，其中推理步骤作为边，从推理路径中总结的语义状态作为节点。GraphPO将语义等价的推理路径合并为等价类，使其能够共享后缀，并将预算从冗余扩展重新分配到多样化探索中。此外，我们为入边分配效率优势，为出边分配正确性优势，从而在从结果中推导过程监督的同时提高推理效率。理论证明GraphPO降低了优势估计方差并提升了推理效率。在三个大语言模型上跨推理和智能体搜索基准的实验表明，在相同令牌预算或响应预算下，GraphPO始终优于基于链和树的基线。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has become a standard paradigm for enhancing the capability of large reasoning models. RLVR typically samples responses independently and optimizes the policy using from final answers. This paradigm has two limitations. First, independently responses often contain similar intermediate reasoning steps, causing redundant exploration and wasted computation. Second, sparse final-answer rewards make it hard to identify useful steps. Tree-based methods partly address this problem by sharing prefixes and comparing branches from the same prefix to provide fine-grained signals. However, tree branches are still expanded independently. When different branches reach similar reasoning states, they cannot share information and repeat similar exploration. Moreover, tree-based methods ignore such dispersion and only perform local comparisons within separate branches, which can lead to higher variance in advantage estimation. To address this challenge, we propose GraphPO (Graph-based Policy Optimization), a novel RL framework that represents rollouts as a directed acyclic graph, with reasoning steps as edges and semantic states summarized from the reasoning paths as nodes. GraphPO merges semantically equivalent reasoning paths into equivalence classes, allowing them to share suffixes and reallocating budget away from redundant expansions to diverse exploration. Furthermore, we assign efficiency advantages to incoming edges and correctness advantages to outgoing edges, thereby improving inference efficiency while deriving process supervision from outcome. Theory shows that GraphPO reduces advantage-estimation variance and enhances reasoning efficiency. Experiments on three LLMs across reasoning and agentic search benchmarks show that GraphPO consistently outperforms chain- and tree-based baselines with the same token budgets or response budgets.

---

## 论文详细总结（自动生成）

# GraphPO：用于推理模型的图基策略优化——详细总结

## 1. 核心问题与研究动机

- **研究背景**：可验证奖励的强化学习（RLVR）是增强大型推理模型（LRMs）能力的标准范式。它通过采样独立响应，根据最终答案的正确性提供二元奖励来优化策略。
- **核心问题**：现有RLVR存在两大局限：
  - **冗余探索**：独立采样中不同响应常包含相似的中间推理步骤，导致计算浪费。
  - **稀疏奖励**：最终答案的二元奖励难以对中间推理步骤进行细粒度信用分配。
- 已有**树基方法**（如共享前缀、比较同前缀分支）部分解决了问题，但仍存在三个不足：
  - 深层树可能鼓励冗余推理步骤，降低推理效率；
  - 早停路径提供有限训练信号，降低利用效率；
  - **树分支独立扩展**，不同分支达到相似语义状态时无法共享信息，仍独立探索，导致冗余。
- **本文目标**：提出图基框架，通过合并语义等价状态，实现更高效、更准确的信用分配和探索。

## 2. 方法论：GraphPO

### 核心思想
- 将推理展开表示为**有向无环图**（DAG）：节点为从推理路径中总结的**语义状态**，边为生成的**推理步骤**。
- 检测并**虚拟合并**语义相似的节点为**等价类**，使等价状态共享后续推理后缀，同时通过路径长度比较实现效率优化。

### 关键技术细节

#### (1) 图构建（Graph Construction）
- **初始化**：以初始提示为根节点`v0`，逐层扩展。每生成一步`u→v`，添加直接边`e(u,v)`。
- **语义状态嵌入**：使用策略模型本身对从根节点到当前节点的整个边路径进行总结，得到语义状态`s_v`，并通过嵌入模型获得向量`z_v`。
- **等价检测**：计算余弦相似度`sim(u,v) = z_u^T z_v / (||z_u|| ||z_v||)`，当大于阈值κ（实验取0.92）时，将非祖先-后代关系的节点合并为等价类`Q(v)`。
- **预算重分配**：当某路径到达已有等价类时，后续扩展可能冗余，将其下一层预算减半（至少保留一个延续），将节省的预算分配给其他路径，提高探索效率。

#### (2) 图奖励（Graph Reward）
- **节点分数**：对于节点v，其自身分数来自其直接可达终端状态的奖励。若v属于等价类，则**池化**等价类内所有节点的分数：
  `S(v) = (c_v^own + w * Σ_{q∈Q(v)\{v}} c_q^own) / (t_v^own + w * Σ_{q∈Q(v)\{v}} t_q^own)`
  其中w是池化系数（实验取0.7），`c_q^own`为节点q直接后缀中正确终端奖励之和，`t_q^own`为总终端数。
- **步级奖励**：对每条直接边`e(u,v)`计算：
  `r_step(u,v) = (S(v) - S(u)) * (1 - η(u,v))`
  其中`η(u,v)=max(0, sim(u,v))`，使步骤在带来正确性提升且语义新颖时获得正奖励。

#### (3) 双组图优势（Dual-Group Graph Advantage）
- **正确性组**：比较同一源节点`u`的不同后继步的奖励。对等价类内共享的后继步，计算标准化的优势：
  `A_cor(e) = (r_step(e) - mean) / std`
- **效率组**：比较到达同一语义状态的不同路径。对等价类`Q`，计算路径长度效率优势：
  `A_eff(v) = \bar{S}_Q * (-ℓ_v - mean(-ℓ_q)) / std(-ℓ_q)`
  其中ℓ_v为从最深共同祖先到v的边数，`\bar{S}_Q`为等价类内节点分数的均值。该优势均匀分配到路径各边。
- **综合优势**：`A_dual(e) = A_cor(e) + λ_eff * A_eff(e)`，λ_eff控制效率信号强度。

#### (4) 策略优化
- 采用类似PPO的裁剪目标，每个token使用相同的图优势，结合KL散度惩罚：
  `J_GraphPO(θ) = E[ (1/|E|) Σ_e (1/|e|) Σ_t min( r_t(θ) A_dual(e), clip( r_t(θ), 1-ε, 1+ε) A_dual(e) ) - β D_KL(π_θ || π_ref) ]`

## 3. 实验设计

### 数据集/场景
- **数学推理基准**：AIME24、AIME25、MATH500。
- **其他推理**：GPQA（研究生级问答）、LiveCodeBench（代码）。
- **智能体搜索任务**：General AI Assistant（GAIA）、WebWalkerQA、BrowseComp、XBench。

### 基线方法
- **标准RLVR**：GRPO、DAPO。
- **树基方法**：TreeRL、SPO、TREE-GRPO、PROS、TreePO。

### 使用模型
- Qwen2.5-7B-Math、Qwen3-8B-Base、Deepseek-R1-Distill-Qwen-7B。
- 智能体任务中还使用Qwen2.5-7B-Instruct配合ReAct框架。

### 训练数据
- 推理任务：DAPO-Math数据集。
- 智能体任务：ASearcher-35K和WebDancer数据。

### 评估指标
- 推理基准：准确率（Accuracy）。
- 智能体任务：各数据集准确率及平均分。

## 4. 资源与算力

- **GPU**：16 × H20 GPU。
- **框架**：基于Verl框架（推理），vLLM用于批量推理；智能体基于Search-R1。
- **训练步数**：推理任务训练350步；智能体任务训练40步。
- **批次大小**：推理512，小批次32；智能体128，小批次64。
- **优化器**：AdamW，学习率1e-6，权重衰减0.01。
- **上下文长度**：Deepseek-R1-Distill-Qwen-7B和Qwen3-8B-Base为16384；Qwen2.5-Math-7B为4096；智能体为8000。
- **注意**：论文未明确说明总训练时长，但提到训练步数和批次大小。

## 5. 实验数量与充分性

- **主要实验**：在5个推理基准×3个模型上对比9种方法（共15组条件），报告准确率。
- **智能体实验**：在4个基准×1个模型上对比4种基线。
- **消融实验**：
  - 合并阈值κ（0.7~1.0）的消融；
  - 池化系数w（0~0.9）的消融；
  - 分支策略T的多种设置；
  - 效率优势的消融（图S.1）。
- **额外分析**：
  - 训练动态（图4：准确率、熵、响应长度）；
  - 与PRM方法的对比（图5a）；
  - 相同轨迹数下的性能与训练时间（图5b）；
  - 轨迹生成效率（图5c）。
- **充分性评价**：实验覆盖多个模型、多个基准、多种基线、关键超参数消融，并在相同token或响应预算下比较。论文还提供了理论分析（附录F）证明方差降低和效率提升。实验设计较为充分、客观、公平。

## 6. 主要结论与发现

- GraphPO在相同token预算或轨迹预算下，始终优于所有基线，在AIME24、AIME25、MATH500、GPQA、LiveCodeBench上均取得最高准确率。
- **探索效率更高**：图结构将冗余探索转化为有用探索（图2c），在训练中熵衰减最慢，保留更多有效推理模式。
- **推理效率更高**：响应长度最短（图4c），效率优势促使策略选择更短的等价正确路径。
- **无需过程注释**：从结果奖励中自导出过程监督，优于需要注释的PRM方法（图5a）。
- **智能体任务同样有效**：在搜索任务上明显优于ReAct基线和树基方法。

## 7. 优点

- **方法创新**：首次将推理展开建模为有向无环图，通过语义合并实现信息共享和预算重分配。
- **双组优势设计**：正确性组提供细粒度步级信用，效率组鼓励短路径，两者结合提升性能与效率。
- **理论支撑**：附录中提供方差降低、探索效率提升、梯度对齐PRM、路径长度优化等严谨证明。
- **实验全面**：覆盖多种模型、多个场景、大量基线，消融实验验证关键设计。
- **实用性强**：在相同预算下，轨迹生成效率与树基方法相当，且无需额外过程注释。

## 8. 不足与局限

- **检测依赖**：语义等价检测依赖于摘要提取器和嵌入模型，虽然理论证明对误检鲁棒，但实践中仍可能引入噪声。
- **阈值敏感性**：性能在κ=0.92附近最佳，偏离时性能下降（图6），需要调参。
- **实验覆盖有限**：主要聚焦推理和智能体搜索，未涉及多模态推理、代码生成、长时交互环境等更广泛场景。
- **计算开销**：构建图需要调用嵌入模型进行相似度比较，虽然实验显示效率可与树基方法媲美，但额外开销在更大规模下可能显著。
- **训练步数较短**：推理任务仅350步，智能体仅40步，可能未完全收敛。
- **超参数**：池化系数w、效率系数λ_eff等需要根据任务调整，未进行系统性灵敏度分析。
- **未公开代码**：论文称代码将开源，但当前无法复现验证。

（完）
