---
title: In-Context Reward Adaptation for Robust Preference Modeling
title_zh: 用于鲁棒偏好建模的上下文奖励自适应
authors: "Zhenyu Sun, Zheng Xu, Ermin Wei"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30323v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 基于Transformer框架建模多样化人类偏好以实现鲁棒RLHF
tldr: 基于人类反馈的强化学习（RLHF）依赖静态奖励模型，难以泛化至多样且未知的人类偏好。本文提出上下文奖励适应框架，利用Transformer的上下文学习能力，从少量偏好示例中动态推断奖励结构，并引入人类响应时间作为辅助信号，克服标准模型的渐近偏差。实验表明，该方法能有效适应未见领域，为异构偏好建模和人机对齐提供可扩展路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30323v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 966, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30323v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30323v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 919, \"height\": 300, \"label\": \"Table\"}]"
motivation: 传统单一奖励模型无法应对多样异构的人类价值，多奖励框架受限固定领域且难以泛化至未知偏好分布。
method: 基于Transformer的上下文奖励适应，从少量示例推理奖励，并融入人类响应时间作为辅助输入。
result: 标准Transformer存在渐近偏差，加入响应时间后模型成功适应未见领域偏好，实现更鲁棒的偏好建模。
conclusion: 该方法提供更鲁棒的偏好建模基础，能够表示异构奖励与分布变化，拓展灵活人机对齐的可行路径。
---

## 摘要
基于人类反馈的强化学习（RLHF）通常依赖静态奖励模型来使大语言模型与人类偏好对齐。然而，人类价值观本质上是多样且异质的，单个奖励模型往往缺乏足够的鲁棒性以泛化到未见过的偏好领域。尽管现有的多奖励框架尝试解决此问题，但它们通常局限于固定的已知领域集合，并且无法在不需要昂贵重训练的情况下适应未见的人类分布。在本工作中，我们提出了上下文奖励自适应，一个基于 Transformer 的框架，旨在动态建模多样且未见的人类偏好。通过利用 Transformer 的上下文学习能力，我们的方法从少量偏好示例中自适应地推断潜在的奖励结构。我们证明，尽管标准 Transformer 架构由于存在对真实值的渐近偏差而不足以完成此任务，但引入人类响应时间作为辅助输入信号可使模型成功适应来自先前未见领域的偏好。我们的发现表明，这种方法为偏好建模提供了更鲁棒的基础，允许表示异质奖励和偏好分布偏移，并为更灵活的人类-AI 对齐提供了一条可扩展的路径。

## Abstract
Reinforcement Learning from Human Feedback (RLHF) typically relies on static reward models to align Large Language Models with human preferences. However, human values are inherently diverse and heterogeneous, and a single reward model often lacks the robustness required to generalize to unseen preference domains. While existing multi-reward frameworks attempt to address this, they are often restricted to a fixed set of known domains and fail to adapt to unseen human distributions without costly retraining. In this work, we propose In-Context Reward Adaptation, a transformer-based framework designed to model diverse and unseen human preferences on the fly. By leveraging the in-context learning capabilities of transformers, our approach adaptively infers the underlying reward structure from a small set of preference demonstrations. We demonstrate that while a standard transformer architecture is insufficient for this task by characterizing an asymptotic bias to the ground-truth, incorporating human response time as an auxiliary input signal enables the model to successfully adapt to preferences from previously unseen domains. Our findings show that this approach provides a more robust foundation for preference modeling, allowing for the representation of heterogeneous rewards and preference distribution shift, and offering a scalable path toward more flexible human-AI alignment.

---

## 论文详细总结（自动生成）

好的，以下是基于所提供论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义
*   **研究动机与背景**：
    *   RLHF（基于人类反馈的强化学习）广泛用于对齐大语言模型与人类偏好，但通常依赖单一的静态奖励模型。
    *   **核心问题**：人类偏好本质上是多样、异质且动态的。单一的奖励模型缺乏鲁棒性，无法泛化至未见过的（OOD）偏好领域。
    *   **现有方法局限**：多奖励模型框架虽然能处理已知的异构偏好，但其领域集合固定，面对新的或变化的偏好分布时，需要昂贵的重训练，缺乏可扩展性和灵活性。
*   **整体含义**：本文旨在提出一种新的范式，利用Transformer的上下文学习能力，在推理时从少量偏好示例中动态推断奖励函数，从而实现对多样且未见的人类偏好的鲁棒适应，推动更灵活的人类-AI对齐。

### 2. 论文提出的方法论
*   **核心思想：上下文奖励适应**：
    *   利用预训练Transformer的上下文学习能力，不更新模型参数，仅通过输入少量（N个）偏好示例（提示），在推理时即适应新用户的奖励模型。
*   **关键技术细节与流程**：
    *   **人类偏好建模**：采用线性奖励参数化 $r_i(x, y) = \phi(x, y)^\top \theta_i^*$ 和 Bradley-Terry (BT) 模型来生成二元比较数据。
    *   **提示矩阵构建**：将N个示例和一个查询实例编码为提示矩阵 $\widetilde{E}_i$，其中每列包含特征差向量 $\tilde{\phi}_l$ 和对应的偏好标签 $z_l$。
    *   **Transformer架构**：采用单层线性注意力Transformer，并对其参数矩阵进行了特定限制以简化分析。其核心预测公式为：
        $$P(\hat{z}_{qi} = 1) = \sigma\left( \frac{1}{N} \sum_{l=1}^{N} z_{li} (\tilde{\phi}_l)^\top U \tilde{\phi}_q \right)$$
    *   **训练**：通过最小化预期交叉熵损失来训练矩阵 $U$。
    *   **核心发现与解决方案**：
        *   **理论局限性**：仅使用二元偏好标签时，偏好信息被非线性压缩，导致对未见过的奖励参数 $\theta_{new}$ 产生**渐近偏差**，理论上无法实现完美的上下文适应。
        *   **引入响应时间**：将人类决策建模为漂移-扩散过程，**响应时间 $t$** 编码了偏好强度信息。结合标签 $z$ 和 $t$，可以获得一个线性信号：$\tilde{\phi}^\top \theta_i^* = \frac{1}{2} \frac{\mathbb{E}[z_i|\tilde{\phi}]}{\mathbb{E}[t_i|\tilde{\phi}]}$。
        *   **恢复可识别性**：在提示中使用比率 $z_i/t_i$ 替代二元标签 $z_i$，并改用平方回归损失进行训练。理论证明，此时模型能够学习到一个可完美解码线性奖励参数的矩阵 $U^*$，从而实现对未见偏好的零偏差适应。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：模拟真实场景，特征向量和人类奖励参数 $\theta^*$ 从特定分布中采样，并模拟漂移-扩散过程生成响应时间。
    *   **真实世界数据集 (Food-Risk)**：来自Smith和Krajbich (2018)的食物风险偏好数据集，包含42名参与者的二元选择、响应时间及眼动数据。
*   **Benchmark与对比方法**：
    *   该方法本身是新框架，其Benchmark是评估模型在**分布内 (ID)** 和**分布外 (OOD)** 偏好上的预测准确性。
    *   **核心对比**：在所有实验中，对比了**“仅用二元偏好标签”（w/o resp）** 和**“融入响应时间”（w/ resp）** 两种设置下的模型性能。
*   **评估模型**：除了理论分析用的**线性注意力Transformer**，还在**GPT-2 (124M参数)** 上进行了验证。

### 4. 资源与算力
*   论文**未明确提及**所使用的具体GPU型号、数量及训练时长。

### 5. 实验数量与充分性
*   **实验数量**：
    *   **合成数据实验**：针对ID和OOD场景，对比了“w/o resp”和“w/ resp”两种设置在两种模型（线性注意力、GPT-2）上的表现，共4组结果。
    *   **真实数据实验**：针对ID和OOD场景，在不同推理长度（M=4, 8, 16）下，对比了“w/o resp”和“w/ resp”两种设置在GPT-2模型上的表现，共6组结果，并包含误差分析图。
*   **充分性与公平性**：实验设计**客观且公平**。它直接验证了核心理论主张，即响应时间能解决二元标签在OOD场景下的失效问题。通过引入真实数据集和不同复杂度的模型，并在统一框架内进行严格的控制变量对比，实验具有较好的代表性。然而，消融实验范围相对有限，主要集中于核心变量的对比。

### 6. 论文的主要结论与发现
1.  **基本不可能性**：仅基于二元偏好标签的上下文学习，即使在大样本极限下，也无法实现对未见人类奖励模型的完美适应，根源在于非线性信息瓶颈。
2.  **响应时间是关键**：引入人类响应时间作为辅助信号，可以恢复偏好强度的线性信号，从而在理论上实现零偏差的OOD上下文奖励适应。
3.  **广泛适用性**：无论是在简化的线性注意力模型还是更复杂的GPT-2模型上，无论是合成还是真实数据集，融入响应时间都能显著提升模型在未见偏好上的鲁棒性，缩小ID与OOD性能的差距。

### 7. 优点
*   **理论深度与创新性**：对上下文学习中二元偏好信息不足给出了严谨的几何解释和定理证明，从根本上阐明了问题所在。
*   **巧妙的解决方案**：受认知科学启发，创新性地将响应时间作为一个易于理论分析和实现的辅助信号，优雅地解决了信息瓶颈问题。
*   **理论与实践结合**：将理论发现同时在简化和真实模型、合成和真实数据上进行了验证，论证链条完整。

### 8. 不足与局限
*   **模型架构简化**：理论证明基于单层线性注意力Transformer，对更复杂的工业级架构（如深层、带Softmax注意力的Transformer）的保证是开放的。
*   **信号采集的局限性**：响应时间在某些实际部署环境中可能难以准确测量或获取，论文对如何获取或处理不可靠的响应时间数据没有讨论。
*   **实验覆盖范围**：
    *   实验未与现有的多奖励模型或元学习方法在处理OOD偏好上进行直接对比。
    *   缺乏对响应时间信号本身质量（如噪声水平）的鲁棒性消融实验。
*   **应用限制**：方法主要针对线性奖励参数化场景，其在更复杂、非线性的真实世界偏好结构上的有效性仍需探索。

（完）
